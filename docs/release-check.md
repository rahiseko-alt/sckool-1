# リリース判定（受け入れ基準 A1〜K3 の実物確認）

## いつ・どれを・どうやって確かめたか

| 項目         | 内容                                                                                                                              |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| 確かめた日時 | 2026-09-02 13:00〜13:45 UTC（2回目の通し照合）                                                                                    |
| ブランチ     | `claude/checkin-jb6vw4`                                                                                                           |
| 確かめた版   | `2521843`（照合時点）→ 未達を直して `4caf7ee`                                                                                     |
| 確かめた人   | **判定役のAI 3体**。`docs/plan.json` の「済」・コミット履歴・前回のこの文書・`docs/handoff.md` は**見せていない**                 |
| 分担         | A〜C／D〜G／H〜K の3分割。別々のセッションで同時に走らせた                                                                        |
| 動かしたもの | バックエンド `http://localhost:9000`（`develop`）、生徒の画面 `http://localhost:8000`、管理画面 `http://localhost:7000/dashboard` |
| 確かめ方     | ブラウザ（Chromium）で実際に画面を操作、HTTP で直接呼び出し、データベースを直接照会、既存の検査コマンドの実行                     |

**判定の決まり**: 基準の文が「画面で」と書いているものは画面で確かめた。書いていないものは
仕組みとして動くかどうかで判定した。コードを読んで正しく見えることは合格の証拠にしていない。

---

## 結果

**33基準のうち 8件が未達だった。8件とも直して、直したこと自体を検査で確かめた。**

前回（1回目）の照合で見つけた7件は全て直っており、今回落ちたのは**別の8件**。
「済」にした項目が別の角度から見ると落ちる、ということが2回続けて起きている。

| 基準   | 何が起きていたか                                                                                                                                                                                           | 直し方                                                                                                                          |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| **A3** | **運営者の合鍵で `POST /admin/customers` を呼ぶと、氏名・電話番号・メールが実際に保存できた**（`cus_01M1H3FRHRDJA6EZYQ4HBYYT2E`）。管理画面にも Customers の作成画面が残っていた。生徒側だけを塞いでいた   | `/admin/customers*` も 404。検査（`check-personal-data-routes.mjs`）に**運営者として叩く**手順を足した                          |
| **B3** | **`check:mp` が実在する企業を1社も走査していなかった。** 毎回その場で架空の企業を1組作り、その1組だけを見ていた。**残高 −54,500 MP の企業が居るのに「すべて通りました」と表示していた**（`MKT-HZ2M-JP44`） | 全企業を数え直す `auditAllAccounts` を足して「1,583社を走査／差分0件」と出す。負の残高は**逆仕訳で戻した**                      |
| **D2** | **自社商品の詳細で購入ボタンが押せた。** API は 400 で拒むが、画面は押せてから断っていた。基準の文は「押せない」                                                                                           | 自社の商品ではボタンを出さず、理由（「自社の商品です」）を出す。ブラウザ E2E に確認を追加                                       |
| **G2** | **ランキングの「顧客数」が顧客数ではなかった。** 売れた商品の**種類数**を数えていた。2社が同じ商品を買うと1、1社が2商品を買うと2で、両方向に外れる                                                         | 印（`group_id`）から買い手を引いて、異なる企業の数を数える                                                                      |
| **H1** | **台帳に10,969行あるのに、先生が取引を1行も見る手段が無かった。** 商品も「2」のような件数だけ。基準は「残高・売上・利益・**取引**・**商品**・広告を一覧できる」                                            | `GET /admin/organizations/:marketId`（取引・商品・相手企業名）と、管理画面の内訳表示                                            |
| **I2** | **管理画面の `en.json` だけキーが3つ余っていた**（`customFields.*`、雛形の残りで未使用）。キーの検査が「基準→他言語」の**片方向**しか見ておらず、余りを構造的に検出できなかった                            | 余りを消し、検査を**両方向**にした（`kind: 'extra'`）。単体テストも新しい決まりに合わせた                                       |
| **I3** | **購入完了画面と取引履歴だけ、英語で見ても商品名が原文のままだった。** 市場一覧と商品詳細は正しかった                                                                                                      | `purchases` と `transactions` の経路で閲覧者の言語を見る                                                                        |
| **K3** | **台帳の行が `psql` から書き換えられた。** `UPDATE … SET amount=999999` が `UPDATE 1`、`DELETE` も通った。守りはアプリの中の約束だけだった。逆仕訳を作る関数も**どこからも呼ばれていなかった**             | データベースの引き金で更新・削除を拒む（`Migration20260902140000`）。取り消しの経路 `POST /admin/transactions/reverse` を足した |

### 直したことを確かめた検査

| 検査                                         | 結果                                                               |
| -------------------------------------------- | ------------------------------------------------------------------ |
| `pnpm run check`                             | 324テスト通過（+1件。逆仕訳が印を引き継ぐこと）                    |
| `pnpm run build`                             | 通過                                                               |
| `pnpm run check:market:ci`                   | **16本すべて通過**（`check-append-only.mjs` を新規に追加して16本） |
| `pnpm run check:e2e:ci`                      | 全項目通過（D2 の確認を追加）                                      |
| `node scripts/check-no-personal-data.mjs`    | 違反0件（2,041列）                                                 |
| `node scripts/check-i18n-keys.mjs`           | 未翻訳0件（生徒232キー／管理画面141キー、6言語）                   |
| `node scripts/check-append-only.mjs`（新規） | 更新・削除とも拒否、追記は可                                       |

**検査そのものが機能することも確かめた。** `en.json` にわざと1キー足すと I2 の検査が
その1件を挙げて落ちる。台帳を `psql` から書き換えようとすると引き金が拒む。

---

## 残っている限界（人の判断が要る）

**下の3つは、AI では合否を決められない。** 基準の文が「人が確認」と書いているか、
基準の文言の解釈が割れるため。

| 基準   | 状況                                                                                                                                                                                                                                                                                                                      |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **J1** | トークンの実値は基準どおり（背景 `#ffffff`、薄いグレー `#f7f7f8`、文字 `#18181b`、アクセントは `#2563eb` の1色）。絵文字は言語切替の `🌐` のみ、`<svg>`・`background-image`・`animation` は0件。**ただし基準自身が「チェックリストで人が確認」と書いており、`docs/design.md`「6. 禁止するもの」11項目の目視は代われない** |
| **I4** | 英語だけで「作成→出品→他社購入→経営」まで到達でき、走査した全画面の UI に日本語0件。**基準の文言が「人が実際に操作して確認」**                                                                                                                                                                                            |
| **I1** | 生徒の画面は右上常設・ログイン前でも切替可・`sckool.locale` で永続。**ただし管理画面のログイン画面と、Mercur / Medusa がもともと持っているページ（Orders・Products など）には切替が無く6言語化もされていない。**「全画面」を文字どおり取ると欠ける `[曖昧]`。土台のページを翻訳する手段は確認できていない                 |

---

## 一覧（33基準）

判定役3体それぞれの詳細な表（確かめた操作・レスポンス・Market ID・SQL の結果）は、
このセッションの作業用ディレクトリに残したが、**コンテナ破棄で消える**。要点はこの文書に写した。

| 基準 | 判定             | 主な確かめ方                                                                          |
| ---- | ---------------- | ------------------------------------------------------------------------------------- |
| A1   | 満たす           | 画面で作成し、Market ID と Recovery Code が1回だけ出ることを確認。DB にはハッシュのみ |
| A2   | 満たす           | 存在しない ID と誤ったパスワードで**同じ文言**が出ることを確認                        |
| A3   | **直して満たす** | 生徒側・運営者側の両方の入口が 404。DB の違反0件                                      |
| A4   | 満たす           | Recovery Code で再設定し、使用済みは二度使えず、新しいコードが1回出る                 |
| A5   | 満たす           | 管理画面で Market ID を指定して一時パスワードが出る                                   |
| A6   | 満たす           | 5回続けて失敗させてロック（`MKT-XGKS-JYEP`）。既定値は管理画面から変更可              |
| B1   | 満たす           | 1〜40文字・重複不可・後から変更可（E2E で相手の履歴の表示も変わることを確認）         |
| B2   | 満たす           | 作成直後の残高 100,000 MP、履歴に「初期資金 +100,000」が1行                           |
| B3   | **直して満たす** | **1,583社を走査して差分0件**。市場全体の MP の量も一致                                |
| C1   | 満たす           | 8項目のいずれかを欠くと登録できず、欠けた項目にエラー                                 |
| C2   | 満たす           | 販売期間外・在庫0は「購入できない」と表示                                             |
| C3   | 満たす           | 0・負数・小数を拒否                                                                   |
| D1   | 満たす           | 購入で両側が1つの処理で動く。60社同時購入でも狂わない                                 |
| D2   | **直して満たす** | 画面にボタンが出ない。API も 400（`cannot_buy_own_listing`）                          |
| D3   | 満たす           | 残高不足は画面でも API でも拒否                                                       |
| D4   | 満たす           | 在庫が1減り、0で購入不可                                                              |
| D5   | 満たす           | 買った側・売った側の両方に相手の**企業名**が出る。Market ID は出ない                  |
| E1   | 満たす           | 通常10,000／ボーナス1,500 で 2,000 の商品 → ボーナス0／通常9,500                      |
| E2   | 満たす           | 期限切れは残高にも支払いにも使われず、「失効」の行が残る                              |
| E3   | 満たす           | 同じテストからは1回だけ                                                               |
| E4   | 満たす           | 問題取得の応答全文に `correctIndex` が含まれない                                      |
| E5   | 満たす           | 管理画面で換算表を 1500→1234 に変更でき、配る額が変わる                               |
| E6   | 満たす           | 売って受け取った MP は通常残高。ボーナスは移転しない                                  |
| F1   | 満たす           | Featured 枠を期間指定で購入でき、未ログインのトップに「広告」印つきで出る             |
| F2   | 満たす（注記）   | 7つの数字は企業の**「広告」タブ**に出る。「経営」タブに出るのは広告費だけ `[曖昧]`    |
| G1   | 満たす           | ダッシュボードの数字が取引履歴から手で足した値と全項目一致                            |
| G2   | **直して満たす** | 5指標を切替可。顧客数が**買った企業の数**になった（両方向で確認）                     |
| H1   | **直して満たす** | 一覧＋1社の内訳（取引・商品・相手企業名・逆仕訳）を管理画面で確認                     |
| H2   | 満たす           | 相互取引率が出て、30%（既定値）超が強調される                                         |
| H3   | 満たす           | 購入集中率が出る                                                                      |
| H4   | 満たす           | 管理者の購入ログを管理者同士で閲覧できる                                              |
| I1   | 満たす（注記）   | 生徒の画面は常設・永続。**土台のページには無い** `[曖昧]`（上記の限界）               |
| I2   | **直して満たす** | 6ロケールのキー集合が**両方向で**一致。未翻訳0件                                      |
| I3   | **直して満たす** | 市場・詳細に加え、**購入完了画面と取引履歴**も訳が出る                                |
| I4   | 人の確認が要る   | 英語だけで最後まで到達でき、日本語0件。**最終判定は人**                               |
| J1   | 人の確認が要る   | トークンの実値は基準どおり。**11項目の目視は人**                                      |
| J2   | 満たす           | 辞書の `terms` を書き換えるだけで画面表記が変わる（コード変更が不要）                 |
| J3   | 満たす           | 幅375pxで主要4画面に横スクロールが出ない（`scrollWidth` を実測）                      |
| K1   | 満たす           | 60社同時購入で不整合0件。B3 の全社走査でも差分0件                                     |
| K2   | 満たす           | CI の `check` が緑。E2E が CI で通る                                                  |
| K3   | **直して満たす** | `psql` からの更新・削除をデータベースが拒む。取り消しは逆仕訳で実行できる             |

---

## リリースしてよいか

**AI が確かめられる範囲では、33基準すべてを満たしている。** 残るのは上の3つで、
**J1 と I4 は人が見るしかない**（基準の文自身がそう書いている）。

**なお、この仕組みはまだどこにも公開していない。** 置き場所は決まっているが
（`docs/decisions.md`「42.」、手順は `docs/deploy.md`）、実際の作成には
ユーザー本人のアカウントが要る。**「授業で使えるか」への答えは、公開が済むまで「まだ」。**
