# 構成の判断記録

`AGENTS.md` と各設定ファイルが「なぜそうなっているか」の記録です。

`AGENTS.md` には運用に必要な指示だけを置き、根拠はこのファイルに分離しています。
**`AGENTS.md` から `@` で import していません。** 毎セッション読み込むとコンテキストを消費し、
指示の遵守率が下がるためです（公式が示す 200 行目安の趣旨）。必要なときに開いてください。

## 凡例

| 印       | 意味                                                                   |
| -------- | ---------------------------------------------------------------------- |
| **公式** | Claude Code 公式ドキュメントに明記があり、それに従った項目。出典を併記 |
| **判断** | 公式に規定がなく、この雛形として選択した項目。変更してよい条件を併記   |

## 記入の作法

設定や指示を変更したら、このファイルの該当行も更新してください。

- **公式** に分類するのは、公式ドキュメントの**原文を読んで確認した**項目だけ。出典 URL を必ず併記する
- **判断** には、理由と「変更してよい条件」を必ず書く。理由のない行は後から判断できないため価値がない
- 確認していない仕様を **公式** に分類しない。確認していなければ **判断** にするか `[曖昧]` を付ける
- 採用しなかった選択肢も「6. 採用を見送った項目」に残す。同じ検討を繰り返さないため

---

## 1. 指示ファイルの構成

| 項目                                                       | 区分     | 根拠 / 理由                                                                                                                                                            |
| ---------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLAUDE.md` に `@AGENTS.md` と書いて import する           | **公式** | [memory](https://code.claude.com/docs/en/memory) — "Claude Code reads `CLAUDE.md`, not `AGENTS.md`" とし、import する例を提示している                                  |
| 文章で「AGENTS.md に従え」と書いても効かない               | **公式** | 同上。`@` から始まるパス記法だけが読み込みを発生させる                                                                                                                 |
| `CLAUDE.md` を import 1行だけにする                        | **判断** | 公式は「Claude 固有の指示を import の下に追記してよい」としている。何も追記していないのは、単一正本を崩さないための選択                                                |
| HTML コメントでメンテナ向けの注記を残す                    | **公式** | [memory](https://code.claude.com/docs/en/memory) — ブロックレベル HTML コメントはコンテキスト注入前に除去される                                                        |
| `AGENTS.md` に書く項目の選定                               | **公式** | [best-practices](https://code.claude.com/docs/en/best-practices) の CLAUDE.md「Include / Exclude」表に準拠                                                             |
| 200 行以内を目安にする                                     | **公式** | [memory](https://code.claude.com/docs/en/memory) — "target under 200 lines per CLAUDE.md file"                                                                         |
| 各行の判定基準は「削ると挙動が変わるか」                   | **公式** | [best-practices](https://code.claude.com/docs/en/best-practices) — "For each line, ask: 'Would removing this cause Claude to make mistakes?' If not, cut it."          |
| ディレクトリ一覧・設計理由・フックの仕組みの説明を置かない | **公式** | 同上 Exclude 列 — "Anything Claude can figure out by reading code" "Long explanations or tutorials" "File-by-file descriptions of the codebase" に該当するため削除した |
| ブランチ名規則・コミット規約は残す                         | **公式** | 同上 Include 列 — "Repository etiquette (branch naming, PR conventions)" に明記されている。削るべきという外部評価が一度出たが、公式の Include 項目のため残した         |
| 整形の設定値を `AGENTS.md` に書かない                      | **判断** | 公式は禁じていない。`.prettierrc.json` と二重管理になり、食い違ったとき判断できなくなるため避けた                                                                      |
| スキル・ルールを空にする                                   | **判断** | `.claude/` は Codex から見えないため、指示を置くと単一正本が分裂する。中身のないサンプルを配る価値もないと判断                                                         |
| サブエージェントは空にしない。`failure-reviewer` を置く    | **判断** | 上記の「空にする」判断を一部撤回した。理由は「9. 失敗行動台帳・放置台帳」を参照。中身のないサンプルではなく、失敗行動台帳を読むために必要な機能のため                  |

## 1-b. 応答と作業の進め方

**この節はすべて判断です。** 公式ドキュメントは応答の態度について規定していません。

| 項目                               | 理由 / 変更してよい条件                                                                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 既存資産の活用を先に検討する       | このリポジトリの所有者の方針。新規実装は保守対象が増えるため最終手段とする                                                                 |
| 迎合しない・率直に述べる           | 同上。同意できない点を飲み込むと、誤った前提のまま作業が進むため                                                                           |
| 確度の低い情報に `[曖昧]` を付ける | 同上。断定と推測が同じ見た目で並ぶと、受け手が検証すべき箇所を判断できないため                                                             |
| 作業報告は「何をどうしたか」だけ   | 同上。経緯と背景は求められたときに出す                                                                                                     |
| 非エンジニア向けに説明する         | 同上。都度「非エンジニア向けに」と指示させるのは非効率。理解できないまま指示・判断させると作業のミスを生み、開発への悪影響の方が大きいため |

雛形として使うときは、チームの方針に合わせて書き換えてください。`AGENTS.md` の
「応答と作業の進め方」が実体で、この節はその根拠です。

「非エンジニア向けの説明」「常に丁寧な態度と言葉遣い」は一度 AGENTS.md から削除した。理由は
[best-practices](https://code.claude.com/docs/en/best-practices) の判定基準
（"Would removing this cause Claude to make mistakes?"）に照らすと、この2件は間違えると
損害が出る指示ではなく好みの範疇で、同表の Exclude 列 "Self-evident practices" に近いため。
外部評価（AGENTS.md が説明過多という指摘）を受けて、AGENTS.md 全体をこの基準で見直した際に
あわせて削った。

その後「非エンジニア向けの説明」はユーザーの指摘により復活させた。理解できないまま指示や
判断をさせること自体が作業のミスを生むという理由は、上記の判定基準（間違えると損害が出るか）
を満たすため、Exclude ではなく Include 側の判断に改めた。「常に丁寧な態度と言葉遣い」は
この理由に該当しないため削除したままにしている。

## 2. 権限（`.claude/settings.json`）

| 項目                                                                     | 区分     | 根拠 / 理由                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ルールの記法（`Bash(cmd *)` `Read(./path)` `:*`）                        | **公式** | [permissions](https://code.claude.com/docs/en/permissions)                                                                                                                                                                                           |
| 鍵ファイルに `Edit` deny も付ける                                        | **公式** | 同上 — "NotebookEdit isn't covered, so add an `Edit` deny rule"                                                                                                                                                                                      |
| `Bash(rm *)`（`rm -rf *` ではなく）                                      | **公式** | 同上 — 最初の `*` より前は文字通り一致するため、`rm -fr` を取りこぼす                                                                                                                                                                                |
| `curl`/`wget` を deny し WebFetch に許可を与える                         | **公式** | 同上 — この2つを一組で推奨している                                                                                                                                                                                                                   |
| deny は allow より優先する                                               | **公式** | [permissions](https://code.claude.com/docs/en/permissions) — 評価順は Deny → Ask → Allow                                                                                                                                                             |
| allow / deny に**何を入れるか**の選定                                    | **判断** | 記法は公式、中身は私の選択。使うコマンドに合わせて各プロジェクトで調整してよい                                                                                                                                                                       |
| 読み取り専用の git を allow に入れる                                     | **判断** | `show` `fetch` `branch` `rev-parse` `ls` を許可。破壊的な部分集合（`branch -d`/`-D`）は deny 優先の原則どおり deny 側で個別に塞ぐ                                                                                                                    |
| `git branch -d`/`-D` を deny                                             | **判断** | `Bash(git branch *)` の allow はブランチ削除も含んでしまう。`rm`/`reset --hard` と同じ形で、破壊的な部分だけ deny に足して塞いだ                                                                                                                     |
| `git push` を ask から allow に移す                                      | **判断** | `--force` と `-f` は deny 済みで deny が allow に優先し、`main` 直 push は Ruleset が止める。二重の確認より許可の回数を優先した                                                                                                                      |
| PR 監視系の `mcp__Claude_Code_Remote__*` を allow                        | **判断** | `subscribe`/`unsubscribe`_pr_activity、trigger の作成・更新・削除・即時実行、`send_later` はコードやリポジトリを変更しない。CI 監視のたびに確認を求められていたため追加した                                                                          |
| `WebFetch` を `code.claude.com` に限定                                   | **判断** | この雛形が公式ドキュメントを参照する前提のため。必要なドメインは追加してよい                                                                                                                                                                         |
| `respectGitignore: true`                                                 | **判断** | 公式の分類では「Interface and terminal」であり、セキュリティ設定ではない。害がないので残しているだけ                                                                                                                                                 |
| サンドボックスを既定で有効化**しない**                                   | **判断** | 公式は defense-in-depth として推奨している。ネイティブ Windows で動かず `allowWrite` が環境依存のため、壊れた既定を配らない選択                                                                                                                      |
| `permissions.defaultMode` を書かない                                     | **判断** | プロジェクト値がユーザー値を上書きするため、全員の auto モードを奪う。`"auto"` はプロジェクトファイルからは効かず選択肢が非対称                                                                                                                      |
| `.claude/` 配下は「保護パス」で `permissions.allow` では事前承認できない | **公式** | [permission-modes](https://code.claude.com/docs/en/permission-modes) の Protected paths 節。`Edit(.claude/**)` を allow に入れても効果がなく、毎回確認を求められる。止めるレバーは `bypassPermissions` モードのみ（deny も含め全プロンプトを止める） |

## 3. フック

| 項目                                                                                        | 区分     | 根拠 / 理由                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 指示は助言、フックは決定論的という区別                                                      | **公式** | [best-practices](https://code.claude.com/docs/en/best-practices) — "hooks are deterministic and guarantee the action"                                                                                                                                                                                            |
| フックの JSON 構造・`async`・`additionalContext`                                            | **公式** | [hooks](https://code.claude.com/docs/en/hooks)                                                                                                                                                                                                                                                                   |
| `SessionStart` の `matcher: "startup\|resume"`                                              | **公式** | [cloud-environments](https://code.claude.com/docs/en/cloud-environments) の例。無いと `/clear` のたびに走る                                                                                                                                                                                                      |
| PostToolUse で整形を走らせるという発想                                                      | **公式** | [hooks](https://code.claude.com/docs/en/hooks) に同型の例がある                                                                                                                                                                                                                                                  |
| `format.sh` / `typecheck.sh` の**実装**                                                     | **判断** | `jq` に依存せず `node` で解析、拡張子フィルタ、成功時は無出力。いずれも私が書いた                                                                                                                                                                                                                                |
| 型検査を `check` ではなく `typecheck` にする                                                | **判断** | Vitest の起動分だけ遅くなるため。テストまで走らせたい場合は差し替えてよい                                                                                                                                                                                                                                        |
| 4フックとも `${CLAUDE_PROJECT_DIR}` ではなく標準入力 JSON の `cwd` へ `cd` する             | **公式** | [hooks](https://code.claude.com/docs/en/hooks) の Worktrees 節 — `${CLAUDE_PROJECT_DIR}` は worktree に入っても元のチェックアウトを指したままだが、`cwd` は worktree のパスに追従する。`AGENTS.md` は当初からこの仕様を書いていたが、フックの実装が追いついていなかった（未検証のまま放置していた）              |
| `typecheck.sh` の `node_modules` 存在チェックを cwd 直下だけでなく祖先ディレクトリも辿る    | **判断** | `EnterWorktree` で実際に worktree に入って E2E 検証したところ発覚。worktree には `node_modules` 自体が無いが、`pnpm run typecheck` はディレクトリを上へたどってメインチェックアウトの `node_modules`（tsc）を見つけて解決できる。旧実装は cwd 直下しか見ておらず、worktree では型検査が黙って no-op になっていた |
| `format.sh`/`typecheck.sh`/`handoff-check.sh` の `cwd` 対応は harness 経由の E2E で検証済み | **判断** | `EnterWorktree` で worktree に入り、実際に Edit で `.ts` を編集して PostToolUse を、worktree を dirty にしてターンを終えて Stop を、それぞれ本物の harness 経由で発火させた。`handoff-stamp.sh`（SessionEnd）だけは実際にセッションを終了させないと発火できないため、サンプル JSON の手動テストのみ              |

## 3-b. セッション間の引継ぎ

既存実装3つ（[392fyc/claude-handoff](https://github.com/392fyc/claude-handoff)、
[shihchengwei-lab/claude-code-session-kit](https://github.com/shihchengwei-lab/claude-code-session-kit)、
[who96/claude-code-context-handoff](https://github.com/who96/claude-code-context-handoff)）を調べ、
書き込み側は2つ目の方式を採った。読み込み側は3つとも SessionStart フックを使っているが、
この雛形では**フックではなく `@` import** にした（理由は下表）。

| 項目                                                                                                      | 区分     | 根拠 / 理由                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| import は再帰的に展開される（最大4段）                                                                    | **公式** | [memory](https://code.claude.com/docs/en/memory) — "Imported files can recursively import other files"                                                                                                                                                                                                                                                                                           |
| プロジェクト直下の指示はコンパクション後も再注入される                                                    | **公式** | 同上 — "Project-root CLAUDE.md survives compaction"                                                                                                                                                                                                                                                                                                                                              |
| プロジェクトのフックは workspace trust を要求する                                                         | **公式** | [hooks](https://code.claude.com/docs/en/hooks) — "Project-level hooks follow workspace trust rules"                                                                                                                                                                                                                                                                                              |
| SessionEnd はモデルを呼べず、予算が既定 1.5 秒                                                            | **公式** | 同上 — "SessionEnd hooks share a 1.5-second budget"。timeout を上げると最大 60 秒まで拡張できる                                                                                                                                                                                                                                                                                                  |
| Stop は毎ターン発火し、exit 2 で会話を継続させられる                                                      | **公式** | 同上 — "Stop fires once per turn"、"exit code 2 prevents Claude from stopping"                                                                                                                                                                                                                                                                                                                   |
| 読み込みは SessionStart フックではなく `@` import                                                         | **判断** | フックは trust 承認まで動かず、コンパクション後の再注入もない。import は両方を満たす。あわせて文章でも読むよう指示している（Codex は import を展開しないため）                                                                                                                                                                                                                                   |
| Stop フックで差し戻す                                                                                     | **判断** | session-kit の方式。スキルは必ず発火しないため、確実性が要るならフックしかない                                                                                                                                                                                                                                                                                                                   |
| 引継ぎは `main` へのマージまでを完了とする                                                                | **判断** | コンテナは終了後に破棄され、次のセッションは `main` を新規クローンする。作業ブランチへのコミットだけでは失われるため、マージしない引継ぎは存在しないのと同じ                                                                                                                                                                                                                                     |
| ただしマージ必須の対象は「引継ぎ文」に限る。コード自体は未完了なら push 済み branch/worktree に残してよい | **判断** | 当初は「main へマージするまで完了」と一体で扱っていたが、これは誤りを含んでいた。**push 済みのコミットは origin に残るため消えない**——消えるのは push していないコミットだけ。次セッションが自動で読むのは `main` の `docs/handoff.md` だけなので、必須なのは引継ぎ文が `main` に載ることで、worktree 越しの継続開発（未完了タスクを branch に残し、次セッションが続行する）を妨げるべきではない |
| `/checkin` と `/checkout` をスキルとして用意する                                                          | **判断** | 作業の開始と終了は人が決めるタイミングであり、フックで検出できない。呼び出し口を用意し、手順を固定した                                                                                                                                                                                                                                                                                           |
| 差し戻しは**1セッションに1回だけ**                                                                        | **判断** | Stop は毎ターン発火するため、無条件に止めると作業にならない。マーカーで抑止している                                                                                                                                                                                                                                                                                                              |
| `docs/handoff.md` を**コミット対象**にする                                                                | **判断** | gitignore にするとクラウドセッション（毎回 fresh clone）で必ず失われるため。複数人で編集すると競合しうる点は許容した                                                                                                                                                                                                                                                                             |
| SessionEnd では機械的な事実だけ記録する                                                                   | **判断** | モデルを呼べないため文章は書けない。ブランチ・HEAD・変更ファイルはシェルだけで取れる                                                                                                                                                                                                                                                                                                             |
| `handoff.md` の役割を「今どの branch を見ればいいか」のポインタに絞る。branch 固有の詳細は書かない        | **判断** | 外部評価の指摘。`main` 上の単一ファイルが複数 branch の状態を同時に代表しようとすると、並列 worktree で片方の更新がもう片方を上書きする。branch 固有の詳細はその branch 自身のコミットや PR 本文に置き、`handoff.md` には列挙用の短いポインタだけを置く形にした                                                                                                                                  |
| 「注意点」に恒久的なリポジトリのルールを再掲しない。`AGENTS.md`/`docs/decisions.md` を参照する            | **判断** | 同上の指摘。実例として、当時の「注意点」に `main` 直 push 禁止・CI ジョブ名の制約など、セッションが変わっても変わらない事実が複数混入していた。これらは既に `AGENTS.md`/`docs/decisions.md` にあり、二重管理は食い違いの元になる                                                                                                                                                                 |
| 同時並行の複数 branch を前提にしたタスク管理層（Issue/Task 単位の状態ファイル等）は追加しない             | **判断** | 外部評価は Goal→Issue→Branch→Worktree→Agent の階層構造を提案したが、この雛形の実運用は直列（1セッション=1系統の作業）であり、使うかどうか未確定の機構を配るのは「AGENTS.md の分量削減」で確立した基準（使わない仕組みを配らない）と矛盾する。複数 branch が同時進行する場合は「次にやること」に箇条書きで列挙する軽量な対処に留め、本格的な状態管理が要るなら再検討する                          |
| `handoff-stamp.sh` は追記ではなく上書き                                                                   | **判断** | `<!-- session-end-stamp -->` 以降を毎回まるごと置き換える実装（`text.slice(0, i) + block`）であることをコード上で確認済み。外部評価は「ログが伸び続ける」懸念を挙げたが、この実装では起きない                                                                                                                                                                                                    |

**この仕組みの限界を明記しておく。** 「セッション終了時にモデルへ引継ぎを書かせる」手段は公式に存在しない。
SessionEnd はモデルを呼べず、Stop はターン単位でしか発火しない。したがって確実に得られるのは
「1セッションに1回の差し戻し」と「終了時の機械的な記録」までで、文章の鮮度は保証されない。
区切りごとに `/handoff` を実行するのが最も確実である。

## 4. 技術スタック

**この節はすべて判断です。** Claude Code 公式ドキュメントはスタックについて何も規定していません。

| 項目                                                | 理由 / 変更してよい条件                                                                    |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Node 22 / TypeScript 5 strict / pnpm / ESM / Vitest | 「鉄板構成」として選択。差し替えは自由。指示ファイルの構成は影響を受けない                 |
| `.npmrc` の `engine-strict` と `.node-version`      | Node 界隈の慣行。`engines` を宣言だけで終わらせないための組み合わせ                        |
| `tsconfig.json` / `tsconfig.build.json` の2分割     | テストを型検査の対象に含めつつ `dist/` に出力しないための一般的パターン                    |
| CI ワークフローの内容                               | GitHub Actions の一般知識。公式の Claude 用 Actions とは別物（後述のとおり同梱していない） |
| ジョブ名を汎用の `check` にする                     | 実行内容が増減しても陳腐化しないため。改名時は Ruleset の必須チェックも同時に更新すること  |

## 5. GitHub 側の設定

**この節もすべて判断です。** GitHub の機能であり、Claude Code 公式の指示ではありません。

- Ruleset（`main` 直 push 禁止、CI 必須、force push 禁止、削除禁止）
- Allow auto-merge、head ブランチの自動削除
- Template repository

`AGENTS.md` の「`main` への直接 push は禁止」を、文章ではなくルールとして強制するために設定しています。

## 6. 採用を見送った項目

いずれも公式に記載があり有用ですが、**雛形として全リポジトリに配ってよいか**という基準で見送りました。
必要なプロジェクトでは各自で追加してください。

| 項目                                                   | 見送った理由                                                                                          |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| Claude Code GitHub Actions（`@claude` 等）             | `ANTHROPIC_API_KEY` か `CLAUDE_CODE_OAUTH_TOKEN` が必須で既定が壊れる。OAuth トークンは個人に紐づく   |
| Code Review（マネージド）                              | Team/Enterprise 限定、組織 Owner の有効化が必要、1レビュー $15-25                                     |
| devcontainer                                           | 公式自身が必須と述べていない。組織ポリシー配布用途は server-managed settings / MDM に格下げされている |
| `security-guidance` プラグイン                         | Node 専用のこの雛形に Python 3.10 と pip の前提が増える。ターンごとにモデル利用コストが発生する       |
| `typescript-lsp` プラグイン                            | 各開発者のマシンに言語サーバのバイナリが必要。未インストールのまま配ると全員にエラーが出る            |
| Notification フック / output styles / ステータスライン | OS 依存または個人の好み。公式も置き場所を `~/.claude/settings.json` としている                        |
| PreToolUse の保護ファイルフック                        | 既存の `permissions.deny` と重複する                                                                  |

導入手順は [README](../README.md) の「導入を検討する価値があるもの」を参照してください。

## 7. テスト方針（test-policy.md）

`docs/test-policy.md` はユーザーが用意した既存文書（TEST_POLICY.md）をそのまま実装したものです。
公式の1標準に基づく指示ではないため、内容は全て **判断** です。ただし内容そのものは、
以下の公式フレームワークが示す考え方と矛盾しないことを WebFetch/WebSearch で確認済みです。

| 項目                                                                   | 根拠 / 照合結果                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 重大度順（P0〜P4）にゲート判定し、上位を優先する構造                   | [ISTQB Glossary — Risk-Based Testing](https://istqb-glossary.page/risk-based-testing/)、[ISO/IEC/IEEE 29119-1:2022](https://www.iso.org/obp/ui/en/#!iso:std:81291:en) — リスクベーステストは重大度・確率でテストの優先順位を決める公式アプローチ                                                                                            |
| P0（人命・認可回避・他テナント漏洩・秘密情報・データ破壊）を最優先     | [OWASP Risk Rating Methodology](https://owasp.org/www-community/OWASP_Risk_Rating_Methodology) — Impact × Likelihood で算出し、深刻度の高い順に対応する手法と整合                                                                                                                                                                           |
| 001〜080 ALL YES を必須リリース条件、081以降は実害があるものだけ       | [Entry and Exit Criteria in Software Testing (Baeldung)](https://www.baeldung.com/cs/testing-entry-exit-criteria)、[Software Quality Gates (testRigor)](https://testrigor.com/blog/software-quality-gates/) — 「欠陥ゼロの証明」ではなく「リスクを許容水準まで下げたら release してよい」という Quality Gate / Exit Criteria の考え方と一致 |
| YES 判定に自動テスト等の客観的根拠を必須とし「たぶん」「理論上」を禁止 | 上記 Exit Criteria 文献の「readiness の客観的な評価尺度」という記述と整合                                                                                                                                                                                                                                                                   |
| 最初の NO で停止し、それだけを直して再判定する運用                     | 特定の公式標準には無い。トヨタ生産方式の Andon／stop-the-line と同型の設計思想で、ISTQB/ISO 29119 が求める水準より厳格。AI エージェントの無限修正ループを防ぐための独自の追加制約として意図的に採用                                                                                                                                         |
| 発見した下位項目を先回りして直さない（Scope Rule）                     | 公式典拠なし。WIP 制限（Kanban）的な「一度に一つ」の運用規律に近い独自ルール                                                                                                                                                                                                                                                                |
| 過剰な将来対応・抽象化・カバレッジ100%化の禁止（修正禁止ルール）       | ソフトウェア工学で広く確立された YAGNI・gold plating 回避の原則と整合。特定の標準機関の文書ではない                                                                                                                                                                                                                                         |

`@` import していない理由は「1. 指示ファイルの構成」の「200 行以内を目安にする」と同じで、
毎セッション読み込むとコンテキストを消費し、指示の遵守率が下がるためです。テストで問題を
見つけたときに、必要な範囲だけ開いて使う想定です。

| 項目                                                                               | 区分     | 根拠 / 理由                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Gate 番号（001〜100）はコードブロックで固定し、Markdown のリストにしない           | **判断** | Prettier は順序付きリスト（`1. 2. 3.`）を自動で振り直す。ゼロ埋め ID を `1. 2. 3.` 形式で書いたところ、Prettier に剥がされて `FAILED_GATE: 003` 等の参照と対応しなくなるバグを実装時に作った           |
| Backlog Rule（Gate 081〜100の未修正記録）の記録先を `docs/neglected-log.md` に統一 | **判断** | 元々は記録先未指定だった。放置台帳と対象が重なり（どちらも「放置してもゴールできる」性質の記録）、書く先が2つあるとAIが毎回迷うため、ユーザーの指示で統合した。詳細は「9. 失敗行動台帳・放置台帳」参照 |

## 8. 調査の範囲と限界

`AGENTS.md` の構成は、以下のページを WebFetch で取得し原文を読んだうえで決めています。

memory / permissions / permission-modes / sandboxing / claude-directory / settings-reference /
best-practices / hooks / skills / sub-agents / mcp / plugins / discover-plugins / output-styles /
statusline / commands / large-codebases / github-actions / code-review / security-guidance /
cloud-environments / web-quickstart / worktrees / devcontainer

**`settings` のページだけは全文を読めていません**（サイズ超過のため先頭のみ）。設定キーの一覧は
`settings-reference` の索引表を根拠にしています。

## 9. 失敗行動台帳・放置台帳

**この節はすべて判断です。** Anthropic/OpenAI の公式ドキュメントが定めた仕様ではありません。
ユーザーが提示した外部研究資料（arXiv の Graph Engineering 論文、NASA Systems Engineering、
Shape Up 等を扱った調査レポート）を踏まえてユーザーと合意した設計です。当の論文自身も
Graph Engineering を「まだ確立していない新興概念（emerging paradigm）」と位置づけており、
公式ベストプラクティスではないことに注意してください。

| 項目                                                                                | 理由 / 変更してよい条件                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/failure-action-log.md` を作る。書きっぱなしでよい                             | 失敗のたびに整理を判断させるとその判断自体の仕組みが要る。書く側の負担をゼロにし、整理コストは読む側（`failure-reviewer`）に寄せた                                                                               |
| 結果ではなく行動を書く                                                              | 結果（バグ・CI赤など）は `docs/test-policy.md` とテスト・CIの担当領域であり、同じものを書くと二重管理になる。テストでは捕まらない「AIの判断・手順のクセ」を残すのがこの台帳の役割                                |
| 失敗したと気づいた、その場で本人が書く                                              | 最終的な解決を待つと、行き詰まったまま終わったセッションでは何も記録が残らない。一番使いたい場面（行き詰まっているとき）で台帳が空になるのを避けた                                                               |
| 正解ではなく、確実な失敗行動を除外するために読む                                    | 台帳には正解は書かれていない。しかし「これは確実に失敗する」と分かっている行動は減らせる。選択肢を絞る消去法としての価値であり、答えを教える機能ではない                                                         |
| 通常のセッションでは読まない                                                        | コンテキストを圧迫するため。読むのはユーザーの明示的指示、または行き詰まって何が悪いか分からないときだけ                                                                                                         |
| 読む前に必ずユーザーへ確認する                                                      | AI がサブエージェントを自分の判断だけで呼び出せると、呼び出し基準が曖昧なまま暴走しうる。ユーザーの承認を必須にした                                                                                              |
| `failure-reviewer` は今の状況に一致する記録を探し、無ければ「該当なし」と報告する   | 一致しない記録を代わりに提示すると、無関係な情報に基づいて判断させてしまう。見つからないことも正直に報告させる                                                                                                   |
| `failure-reviewer` は正解・戦略を提案しない                                         | 「失敗行動を報告する役割」と「それを踏まえて次にどう動くか判断する役割」を分離した。後者は呼び出し元（本編のAI）の役割                                                                                           |
| `docs/neglected-log.md` を作る。ゴール到達時のみ読む                                | 放置したままゴールできたなら致命的ではなかったことの裏付けになる一方、ゴール定義自体が最初から完全ではないため、放置事項に未知の重大リスクが紛れる可能性がある。リリース前に一度だけ棚卸しする安全弁として置いた |
| `docs/test-policy.md` の Backlog Rule も `docs/neglected-log.md` へ統合             | 両者とも「放置してもゴールできる」性質の記録で、記録先を分ける理由がない。分けたままだとAIが毎回どちらに書くか迷う。ユーザーの指示で統合した                                                                     |
| 書くきっかけを「Gateに触れたが破らなかった（ヒヤリハット）」に限定                  | 書く・書かないの基準を「放置しても次に進めるか」で統一。Gateを突き破ったものは即座に直すため放置できない。触れてもいないものは記録する価値がない。ユーザーの提案（ガードレールの比喩）をそのまま採用             |
| 書く内容を「日時・触れたGate・対象箇所」の3点に限定し、理由は書かせない             | 理由を書かせると読み手が「その理由が正しいか」を推測で判断することになり、test-policy.mdのEvidence Rule（証拠なき`たぶん`の禁止）と矛盾する。「いつ・どこ」だけを渡し、読み手が実際に確認しに行く設計にした      |
| ゴールは大計画に対して1つ。「問題がなければ即リリースできる状態」だけをゴールと呼ぶ | ユーザーの明示的な定義。Node／branch 単位の部分完了をゴールと呼ばない                                                                                                                                            |
| 台帳2ファイルに `.gitattributes` で `merge=union` を設定                            | 複数 branch が並行して同じファイルの末尾に追記する運用のため、素の3-way mergeでもコンフリクトは起きにくいが、両者の追記を確実に両方残すため明示した                                                              |
| `failure-reviewer` サブエージェントを追加                                           | 「1. 指示ファイルの構成」で一度見送った「サブエージェントを空にする」方針を、この機能に限り撤回した。中身のないサンプルではなく、失敗行動台帳の整理という具体的な必要から追加した                                |

### 検討したが採用しなかった部分

「失敗の種類（カテゴリ）」と「重大度のグラデーション」を、`docs/test-policy.md` の P0〜P4
構造を転用して失敗行動にも導入する案を検討した。しかし「結果ではなく行動を書く」という、
より明確で運用コストの低い軸が見つかったため、種類・グラデーションによる分類は採用しなかった。
将来、記録が増えて分類が必要になったら再検討する。

### 保留にした部分：Task Graph（計画の構造・番号）

Task Graph（計画の構造）／Runtime State（今どのタスクが進行中・完了・失敗かの機械的な記録）／
セッション開始時に計画と現在地を自動把握する仕組みは、**まだ実装していません。** ただし
設計自体はユーザーとの議論でかなり具体的なところまで合意できているので、次回セッションが
ゼロから考え直さずに済むよう、到達点をここに記録します。

#### 合意できている設計

| 項目             | 内容                                                                                                                                                                                                                                                                             |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 並列モデル       | グラフエンジニアリング方式（仕事＝グラフが先にあり、担当は都度組む）。「AI会社方式」（組織が先）は不採用                                                                                                                                                                         |
| ラインの分割基準 | 依存関係テストのみ（一方の成果物をもう一方が入力として必要とするか）。ファイルが被るかどうかは基準にしない。**公式**：Anthropic公式ブログ（"tasks don't depend on each other"）、OpenAI公式Agents SDK（同様）、Claude Code公式（worktreeで隔離すれば着手のブロッカーにならない） |
| 番号体系         | ライン＝英字（A, B, C…）。フェーズ・ステップ＝数字。フェーズはライン内で独立カウント（ラインAのフェーズ1とラインBのフェーズ1は別物）。1フェーズ中ステップ1〜9が基本、10以上で新フェーズに分割                                                                                    |
| 担当の扱い       | 番号に含めず、ラインごとの別項目として記録する。**公式**：Dynamic Workflows・Agent Teamsとも識別子（label／状態＋依存関係）と担当（assign／self-claim）を分離している                                                                                                            |
| 寄り道           | フェーズ-ステップ-寄り道N（最大9）。「寄り道とは何か」の定義はあえて決めない（AIが定義に無理やり当てはめる弊害を避けるため）。9を超えたら寄り道ではなく計画見直しレベル                                                                                                          |
| 進捗表示         | ラインごとの分数＋全体合計を並べる。分母（フェーズ×ステップ数）は事前固定。寄り道は分母に含めず、別枠で「現在の寄り道 N/9」を常時表示（数字が止まって見えることの方が、変化が見えることより不安を生むため）                                                                      |
| 依存関係の記述   | 各フェーズに成果物（完成すると何ができるか）を一言で明記させ、依存があるフェーズには「フェーズ◯の『（成果物名）』を使う」と書かせる。非エンジニアが専門知識なしに文章で確認できるようにする狙い                                                                                  |
| 依存関係の検証   | 3体のAIが異なる視点（技術的／業務フロー／リスク）で敵対的に検証し、各視点が賛成理由・反対理由の両方を出す                                                                                                                                                                        |
| ファイル形式     | `test-policy.md`と同じくコードブロックで番号を固定する（Prettierの自動採番バグを避けるため）                                                                                                                                                                                     |

#### 敵対的検証（3視点AI）で見つかった、実装前に対処が必要な4つの欠陥

上記の設計案そのものを、技術的／業務フロー／リスクの3視点で敵対的に検証したところ、
複数の視点が独立に指摘した重大な欠陥が4つ見つかった。**これらを解決してから実装すること。**

1. **計画ファイルの並列編集問題**（技術的視点＋リスク視点が指摘）。複数AIが同時に「全体進捗」や
   依存先フェーズ番号を書き換えると、更新が消えたりズレた依存参照が残ったりする。これは
   `docs/handoff.md`で一度指摘・修正した「単一ファイルが並列worktreeと両立しない」問題の再発
2. **再採番で過去の参照が壊れる**（技術的視点＋リスク視点が指摘）。フェーズ分割（ステップ10以上）
   や計画見直し（寄り道9超過）が起きると、既存の番号（失敗行動台帳・放置台帳・PR・コミットが
   引用している番号）との対応が壊れる。番号は不変という前提と、動的に分裂しうる設計が矛盾
3. **計画者＝実行者＝完了判定者が同一AI**（リスク視点＋業務フロー視点が指摘）。敵対的検証は
   「依存関係の申請」だけが対象で、「本当に完了したか」「これは本当に寄り道か」という自己申告
   部分は誰も検証していない。数字操作（ステップを細かく刻んで進捗を良く見せる等）を防ぐ仕組みが
   構造的にない
4. **「寄り道の定義を決めない」ことが抜け道になる**（業務フロー視点＋リスク視点が指摘）。AIを
   型にはめない工夫だったが、同時に「何が寄り道かをAI自身が決められる」抜け道でもある。本体の
   進捗を止めたくないAIが、問題を寄り道側に逃がすインセンティブが生まれうる

単独の視点からの指摘（優先度は下がるが記録しておく）: 分母固定がフェーズ分割時に「後退した
ように見える」逆効果を生みうる（業務フロー視点）／"Task Graph"と称する割に依存関係を機械的に
追跡する実体（グラフ構造）がない、ラベルにすぎない（技術的視点）／敵対的検証の結果を読む
強制チェックポイントがなく形骸化しうる（技術的視点＋リスク視点）／表示が「一点だけ見たい」
というユーザーの要求と裏腹に4階層の数字が並び情報過多（業務フロー視点）。

次回セッションは、上記4つの欠陥への対処案をユーザーと詰めてから実装に進むこと。

#### 一次情報の調査結果（2026-08-30）

「4つの欠陥を個別に潰すのではなく、計画の立て方／立てるタイミング／全体計画とセッション実行
計画の境界／セッション粒度を先に確定すれば解決できるのではないか」というユーザーの指摘を受け、
一次情報（公式ドキュメント・公式リポジトリ）を調査した。3系統が同じ3層構造に収束していた。

| 層           | Anthropic 長時間エージェント        | GitHub spec-kit                         | 性質                   |
| ------------ | ----------------------------------- | --------------------------------------- | ---------------------- |
| 動かない     | （CLAUDE.md 相当）                  | `constitution.md`（不変の原則）         | 恒久ルール             |
| 長生きする   | `feature_list.json`                 | `spec.md`（"the primary artifact"）     | 全体計画。書き換えない |
| 毎回作り直す | 各セッションが未完の最優先を1つ選ぶ | `plan.md` / `tasks.md`（"Regenerable"） | 実行計画。使い捨て     |

主な原文根拠:

- **計画は一度だけ作る**: 初期化専用エージェントが機能要件ファイルを一度書く。形式は JSON
  （"the model is less likely to inappropriately change or overwrite JSON files compared to
  Markdown files"）。実行側は `passes` フラグの真偽値だけを変える。
  出典: Anthropic「Effective harnesses for long-running agents」
- **セッション粒度は「1回に1機能」**: 一括実装（one-shot）でコンテキストが尽きる失敗を経て
  "work on only one feature at a time. This incremental approach turned out to be critical."
  1単位の大きさは end-to-end の検証手順を書ける大きさ。同上
- **セッションをまたいで残すのは4つ**: 進捗ファイル／説明的な git コミット履歴／完了フラグ付き
  機能リスト／環境復元スクリプト。"compaction doesn't always pass perfectly clear instructions
  to the next agent" として自動圧縮任せを明確に否定。同上
- **番号は不変・追記のみ**: 既存タスクIDの最大値 M を求め `T{M+1:03d}` から採番。既存タスクの
  書き換えは MUST NOT。残作業ゼロなら "byte-for-byte unchanged"。
  出典: GitHub spec-kit `/speckit.converge`
- **並列可否は計画時に静的判定**: `[P]` マーカーは "different files, no dependencies" のときだけ
  付ける。禁止事項に "same file conflicts" が明記。出典: spec-kit `tasks-template.md`
- **完了判定は作業者から分離**: "a verification subagent ... so the agent doing the work isn't
  the one grading it"。証拠（テスト出力・実行コマンドと戻り値・スクリーンショット）を出させる。
  出典: Claude Code 公式ベストプラクティス。spec-kit `/speckit.converge` は git 履歴もチェック印も
  見ずコード実体だけを見る（"no git, no branch comparison, no history"）
- **観測された自己申告の失敗**: 「進んでいるのを見て勝手に完了宣言する」「十分にテストせず完了
  マークを付ける」「ユニットテストや curl は通るが end-to-end では動いていないことに気づかない」。
  出典: Anthropic 長時間エージェント記事
- **計画が不要な下限**: "If you could describe the diff in one sentence, skip the plan."
  出典: Claude Code 公式ベストプラクティス
- Claude Code の Plan mode は計画の保存先を公式に規定していない（＝セッション内限りの道具）。
  セッションをまたいで残る公式の仕組みは `CLAUDE.md` / auto memory (`MEMORY.md`, 先頭200行または
  25KB) / `/goal` の3つ。サブエージェントは親の会話履歴・読んだファイル・auto memory を一切
  引き継がない（fork を除く）

#### 上記を踏まえた設計変更案（未合意・ユーザー確認待ち）

4つの欠陥への対処:

| 欠陥             | 対処                                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------------------- |
| ① 並列編集       | 計画ファイルへの書き込みを「末尾への追記」と「自分の担当項目の状態1箇所」だけに制限。全体進捗は誰も書かず数えて導出 |
| ② 再採番         | 番号は不変。分割・追加は最大値+1で末尾に追記。「ステップ10以上で新フェーズに分割」を廃止                            |
| ③ 自己申告       | 完了判定を、状態欄も git 履歴も見ないまっさらな別AIに委譲。「確かめ方」の手順を実際になぞらせ、証拠を出させる       |
| ④ 寄り道の抜け道 | 寄り道枠を廃止。計画外作業は末尾に番号付きで追記する（逃がす先がなくなる）                                          |

元の設計から捨てることになるもの:

1. 4階層の番号体系（ライン→フェーズ→ステップ→寄り道）→ 2階層（項目番号のみ、不変・連番）へ。
   ラインは番号から外し、並列可否マーカーという属性にする
2. フェーズの動的分割（ステップ10以上）→ 廃止
3. 寄り道枠（最大9）→ 廃止
4. 「依存関係テストのみで分割、ファイル重複は基準にしない」→ 公式はファイル重複も並列判定に
   入れている（spec-kit の "same file conflicts"）。要再検討
5. 分母の事前固定 → 追記で分母は増える。ただし「当初計画 X/Y」「追加分 A/B」を分けて表示すれば
   当初計画分は後退しない

維持するもの: グラフエンジニアリング方式、担当を番号から分離、依存関係を文章で明記して非エンジニア
が確認できるようにする、3視点の敵対的検証、コードブロックで番号を固定する。

計画ファイルの具体的な書式案（会社紹介サイトを題材にした例）:

```
T001  [並列可]  トップページの骨組み
      できるもの : ヘッダー・本文枠・フッターが並んだHTML 1枚
      確かめ方   : ブラウザで開く → 画面幅をスマホ相当まで狭める
                   → 文字が画面からはみ出していないこと
      依存       : なし
      触るファイル: index.html
      状態       : 済
```

「確かめ方」は人間が画面でたどれる手順で書く（「テストが通る」ではなく「押す・見る」）。
これを書けない項目は、粒度が大きすぎるというシグナルになる。

#### 未解決のまま残る点

- **複数AIが同時に同じ計画ファイルを触るときの排他制御は、どの一次情報にもない `[曖昧]`。**
  spec-kit は「別ファイルのタスクだけ並列可」にしてそもそも競合させない方向で回避しており、
  ロック機構を持たない。書き込みを追記のみに制限すれば実害はほぼ消えるが、確認はできていない
- 全体計画のファイル形式を JSON（書き換えにくいが非エンジニアに読みにくい）にするか、Markdown の
  コードブロック（読みやすいが書き換えられやすい）にするか
- 完了判定を `/checkout` に組み込むか、独立したサブエージェントにするか
- 1つの全体計画に項目をいくつ置くか（Anthropic の実例は200項目以上）

#### 検証の層構造（2026-08-30 の続き。未実装・未合意）

「完了判定を別 AI にやらせる」を `/checkout` に置くと**そのセッションで作った 1 項目しか
見ない**、では 100 項目の検証リストはいつ使うのか、というユーザーの指摘から整理した。
検証には性質の異なる 3 つの場面があり、これを混同していた。

| いつ           | 何を検証するか                     | 誰が                  | コスト   |
| -------------- | ---------------------------------- | --------------------- | -------- |
| セッション終了 | 今回やった 1 項目だけ              | まっさらな別 AI       | 小・毎回 |
| セッション開始 | 前回までの成果が今も動くか、ざっと | 着手する AI 自身      | 小・毎回 |
| 全体照合       | 全項目                             | 別 AI（書き込み不可） | 大・節目 |

**全体照合が必要な理由はデグレ検出。** ある項目を「済」にした後、別の項目の作業でそれが
壊れても、セッション単位の検証は構造的に検出できない（「済」の項目はもう誰も見に行かない
ため）。全体照合は「状態」欄も git 履歴も見ず、コード実体だけを「確かめ方」と突き合わせる。
**公式**：spec-kit `/speckit.converge` は「does not track changes ... no git, no branch
comparison, no history」、仕様を「the sole source of intent」として現在のコードと照合し、
missing / partial / contradicts / unrequested に分類する。書き込みは末尾追記のみ。

**CI が緑でも意味がない場合への公式の対処は「単一チェックの精度を上げる」ではなく
「性質の違う層を重ねる」。** Claude Code 公式は検証を強度順に 4 段階で並べており、最上位は
「a fresh model try to refute the result, so the agent doing the work isn't the one
grading it」（まっさらなモデルに反証させる）。1〜3 は「決めた手順が通るか」を見るのに対し、
4 は「間違っているのでは」を能動的に探す点で性質が違う。失敗パターンには
「The trust-then-verify gap」という名前が付いており、対処は
「If you can't verify it, don't ship it.」。

**見えない範囲は潰さず記録する。** Anthropic は Puppeteer 経由でブラウザ標準の警告ダイアログ
が見えないことを把握していたが、検証ツールを改良せず「it can't see ... features relying on
these modals tended to be buggier as a result」と書き残しただけだった。調査システムでも
ルーブリックによる LLM 採点をすり抜けた問題（SEO 最適化された内容の薄い情報源を選ぶ傾向）を
最終的に捕まえたのは人間のテスターだった。**このリポジトリの放置台帳・失敗行動台帳と同じ
発想であり、整合する。**

対処案：全体計画の各項目に「自動化」欄を持たせ、CI で確認できるものと人間が確認するしかない
ものを区別する。人間が見るのは後者だけで済み、「CI が緑でも誰も見ていない項目」が一目で分かる。

**注意点**：反証させる別 AI には副作用がある。公式が明記している——「A reviewer prompted to
find gaps will usually report some, even when the work is sound」。対策は
「flag only gaps that affect correctness or the stated requirements」。既存の
`failure-reviewer` サブエージェントを「正解・戦略は提案しない」と縛っているのと同じ発想。

**新たに増えた未決の点**：

- 「自動化」欄を 2 段階（CI / 人間）にするか、間に「別 AI が画面操作で確認できる」を挟んで
  3 段階にするか
- 全体照合を 10 項目ごとに走らせるか、大計画のゴール到達時だけにするか
- 全体照合で壊れが見つかったとき、既存項目を「済」から「未」に戻す（進捗が後退する）か、
  新番号で末尾に追記する（進捗は後退しないが同じものが 2 箇所に載る）か

#### 非エンジニアが確認する場合の設計（2026-08-30 の続き。未実装・未合意）

「AI は信用できない、CI は見ない、人間は非エンジニア。誰が検証するのか」というユーザーの
指摘から整理した。**前提を1つ逆転させることで解ける。**

**非エンジニアはボトルネックではなく、唯一の正解保持者。** 会社概要ページの本社住所が
引っ越し前のままでも、料金表が ¥3,000 のところ ¥300 でも、**AI も CI も永久に気づかない**。
人間だけが「何が正しいか」を知っている。これは非エンジニアが最も得意な検出であり、
同時に最も価値の高い検出でもある。

**したがって聞き方を変える。人間には「これで合っていますか」しか聞かない。
「このコードは正しいですか」は絶対に聞かない。** 後者は確認ではなく責任の押し付けで、
非エンジニアは「はい」と答えるしかなく検証として機能しない。

聞き方の要件は3つ:

- **証拠はスクリーンショット・実物にする。ログやテスト出力は人間に見せない。**
  **公式**：「Have Claude show evidence rather than asserting success: ... or a screenshot
  of the result」「do all testing as a human user would」
- **選択肢つきの質問にする**（例:「受信箱に届いていますか？ はい／いいえ／届いたが内容が
  おかしい」）。専門知識ゼロで確実に答えられる形にする
- **人間が見るのは「自動化」欄が★の項目だけ。** 100 項目中 5 件程度に絞る

**「AI を信用しない」の実装は「疑う」ではなく「証拠を出させる」。** 疑うのは人間の労力を
食うので非エンジニアには続かない。証拠を出させれば労力は AI 側に寄る。人間に届く前に、
作った本人でない別 AI が反証して大半を落とす。

**CI は人間が見なくてよい。** 緑でも赤でも人間の行動は変わらない（赤なら AI が直すまで
待つだけ）。CI は人間向けではなく、AI が自分の仕事を確かめるための道具である。

**それでも 3 層をすり抜ける失敗が残る**（今は動くが作りが脆い／セキュリティ的に危ない）。
AI は自作なので気づきにくく、CI は「今動くか」しか見ず、非エンジニアには構造上見えない。
**公式にもこれへの答えはない `[曖昧]`**（Anthropic は専門エージェントへの分業で改善しうるが
未確定と明記して止めている）。対処は「潰す」ではなく「受け入れたと記録する」——
既存の `docs/neglected-log.md`（放置台帳）がこの役割を果たし、ゴール到達時に読む。
Anthropic が Puppeteer の盲点を直さず書き残したのと同じ扱い。

**新たに増えた未決の点**：★の項目を人間に聞くタイミングを、溜めて一括（10 項目ごと、または
ゴール到達時）にするか、出たらすぐにするか。溜めても確認待ちの項目は「未」のまま止まるので
進捗の数字は嘘にならない。

なお「自動化」欄は **2 段階（CI で確認できる／人間が確認するしかない）で確定**。3 段階目
（別 AI が画面操作で確認できる）は項目の性質ではなく作業の進み具合を表すため採らない。

#### ユーザーが確定させた点（2026-08-30）

議論の結果、次の 4 点が確定した。**これ以外はまだ未決であり、実装には入っていない。**

| 項目                        | 決定                                                                                               | 理由                                                                                                                         |
| --------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 全体計画のファイル形式      | **JSON**                                                                                           | **公式**：Anthropic「the model is less likely to inappropriately change or overwrite JSON files compared to Markdown files」 |
| 「自動化」欄                | **2 段階**（CI で確認できる／人間が確認するしかない）                                              | 3 段階目（別 AI が画面操作で確認できる）は項目の性質ではなく作業の進み具合を表すため                                         |
| ★項目を人間に聞くタイミング | **溜めて一括**                                                                                     | ★は元々少ない。1 件ずつ呼ぶと続かない。溜めても確認待ち項目は「未」のまま止まるので進捗の数字は嘘にならない                  |
| 3 層構造と番号の扱い        | 恒久ルール／全体計画（不変・追記のみ）／使い捨ての実行計画。番号は不変、追加は最大値 +1 で末尾追記 | **公式**：Anthropic と spec-kit が独立に同じ構造へ収束。spec-kit `/converge`「既存タスクの書き換えは MUST NOT」              |

**JSON を選んだことで生じる副作用**：非エンジニアが読みにくくなる。`AGENTS.md` の
「非エンジニア向けに説明する」と衝突するため、**JSON をそのまま読ませない前提で設計すること**
（人間が見るのは★項目の確認画面と進捗の数字だけで、計画ファイル本体は AI が読む）。
この点は実装時に必ず確認する。

#### 残っていた 5 点も確定（2026-08-30）

ユーザーが AI 側の推奨をすべて採用した。**これで設計上の未決はなくなり、実装に入れる状態。**

| 項目                     | 決定                                                                     | 理由                                                                                                                                                                         |
| ------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 完了判定の置き場所       | **独立したサブエージェントを作り `/checkout` から呼ぶ**                  | `/checkout` に直接書くと「早く終わらせたい」文脈と同居する。既存の `failure-reviewer` と同じ形にできる                                                                       |
| 並列可否の判定基準       | **ファイルの重複も入れる**（既存の合意を変更）                           | worktree で隔離しても `main` へのマージ時に衝突する。**公式**：spec-kit は `[P]` の条件を「different files, no dependencies」とし、禁止事項に「same file conflicts」を挙げる |
| 全体計画の項目数         | **数を先に決めない。「1 項目＝1 セッションで終わる大きさ」を基準にする** | 数を先に決めると粒度をそれに合わせて歪める動機が生まれる。目安は初回 30〜50（Anthropic の実例 200 以上はアプリ 1 本を丸ごと作る規模）                                        |
| 全体照合の頻度           | **10 項目ごと＋ゴール到達時の両方**                                      | デグレは発見が遅れるほど原因特定が難しい。「自動化＝CI」の項目は CI が毎回見ているので、実際になぞるのは★項目だけで済む                                                      |
| 全体照合で壊れが出たとき | **「済」は戻さず、末尾に新番号で追記**                                   | 番号不変の原則と一貫する。「済」を「未」に戻すと同じ項目が 2 回「済」になり履歴が壊れる。追記なら「一度完成し、後で直した」事実がそのまま残る                                |

#### 実装時に持ち越す注意点

- **JSON をそのまま人間に読ませない。** 人間が見るのは★項目の確認画面と進捗の数字だけ
- **人間への質問は選択肢つきの YES/NO にする。** 証拠はスクリーンショットや実物で示し、
  ログやテスト出力は見せない。「このコードは正しいですか」は聞かない
- **完了判定サブエージェントには範囲を縛る指示を入れる。** **公式**が警告している——
  「A reviewer prompted to find gaps will usually report some, even when the work is sound」。
  対策は「flag only gaps that affect correctness or the stated requirements」。
  既存の `failure-reviewer` を「正解・戦略は提案しない」と縛っているのと同じ発想
- **複数 AI が同じ計画ファイルを同時に触るときの排他制御は一次情報になく `[曖昧]` のまま。**
  書き込みを「末尾追記」と「自分の担当項目の完了フラグ 1 個」に限定することで実害はほぼ消える
  想定だが、確認はできていない

### 10. 全体計画（`docs/plan.json`）の実装

設計は「9. 失敗行動台帳・放置台帳」内「保留にした部分：Task Graph」以下に記録した確定 9 点に
従った。ここには**実装で下した追加の判断**だけを書く。

| 判断                                                   | 理由                                                                                                                                                                    |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 検証を `src/plan.ts` の関数として書き CI で回す        | 「番号は不変・追記のみ」は人間の注意力では守れない。id の昇順チェックは、既存項目を書き換えたことの検出を兼ねる。**自動化できるものは CI へ**という自分たちの方針の実践 |
| 並列可否をフィールドに持たず `canRunInParallel` で導出 | `dependsOn` と `files` から決まる値を別に持つと、両者が食い違う。導出なら矛盾しようがない                                                                               |
| 進捗もフィールドに持たず `countProgress` で導出        | 複数 AI が同時に合計値を書き換えると競合する（欠陥①）。数えれば必ず正しい                                                                                               |
| `status` に `awaiting_human` を設けた                  | 「AI 側は終わったが人間の確認待ち」を `done` と区別する。溜めて一括で聞く（確定事項）ために必要で、かつ確認待ちを完了に数えないので進捗の数字が嘘にならない             |
| `origin` に `initial` / `added` を持たせた             | 追記で分母が増えても「当初計画 3/8」が後退しない。3 視点検証で出た「分母固定が後退に見える」という指摘への対処                                                          |
| `verify` と `files` を空にできない制約                 | 確かめ方を書けない項目は粒度が大きすぎる。触るファイルが分からない項目は並列可否を判定できない。どちらも空欄を許すと設計全体が空洞化する                                |
| 検証は最初の違反で打ち切らず全件返す                   | 書いた人が一度に全部直せるようにするため                                                                                                                                |
| 実際の `docs/plan.json` は雛形に含めない               | 雛形の時点では中身が決められない。`docs/plan.example.json` を見本として置き、テストは実ファイルが存在するときだけ検証する（`describe.skipIf`）                          |

**追加したファイル**：`src/plan.ts` / `src/plan.test.ts` / `src/plan.file.test.ts` /
`docs/plan.example.json` / `.claude/agents/completion-checker.md` /
`.claude/skills/plan-init` / `.claude/skills/plan-verify` / `.claude/skills/plan-ask`。
`/checkin` に「次の1項目を選ぶ」、`/checkout` に「完了判定を受ける」を追加した。

**`AGENTS.md` に「全体計画」節を追加した理由**：`.claude/` 配下は Codex から見えない。
サブエージェントとスキルに閉じた運用ルールは Codex が守れないため、正本を `AGENTS.md` に置く。

**残る `[曖昧]`**：複数 AI が同時に `docs/plan.json` を編集するときの排他制御。書き込みを
「末尾追記」と「自分の担当項目の `status` 1 個」に限定したことで実害はほぼ消える想定だが、
一次情報に記述がなく確認できていない。

**未検証の懸念を `AGENTS.md` に直接書いた理由**：別台帳を作らない、というユーザーの判断。
台帳を増やすと読むきっかけの設計（いつ読むか、誰が読むか）が新たに必要になるうえ、
この懸念は「全体計画を使うたびに目に入るべきもの」で、`AGENTS.md` は毎セッション読まれる。
**懸念が外れたら、その場で `AGENTS.md` の該当行を書き換える**運用にした（記録を残すこと
自体が目的の台帳とは、この点で性質が違う）。

書いた懸念は6つ。完了判定役が作った側に引きずられないか／`verify` の質が項目数とともに
劣化しないか（**`verify` の中身は CI で検査できない**、空欄しか見ていない）／項目が増えた
ときの読み込みコスト／全体照合の実所要時間が未計測／排他制御／3層をすり抜ける脆さ。

### 11. チェックイン／チェックアウトの既定動作

ユーザーの指示で、次の3点を既定にした。**いずれも「聞かない」「黙らない」方向の変更**。

| 変更                                                     | 理由                                                                                                                                                                         |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/checkin` でこのセッションのスコープを報告する          | 「終わったと判断できる線」を着手前に引かないと、どこまでやっても終わらず、コンテキストが尽きた時点で中途半端に切れる。`docs/plan.json` があれば選んだ1項目がそのままスコープ |
| スコープ達成後は聞かずに `/checkout` まで実行する        | 「チェックアウトしますか？」と聞く間にセッションが切れると、引継ぎが `main` に入らず成果が次から見えなくなる。例外は「判断待ち」と「達成できない」の2つだけ                  |
| `/checkout` で「消失」を必ず報告する（無ければ「なし」） | コンテナは終了後に破棄され `origin` に無いものは全て消える。**書かないことと「無い」ことは読み手に区別がつかない**ため、「なし」の明示を必須にした                           |

消失の判定は思い込みでなく実際のコマンドで行う（`git status --short` /
`git log --branches --not --remotes` / `git stash list` / `git worktree list`）。
**push 済みのブランチは `main` へマージしていなくても残る**点を表で明示し、
「マージしていない＝消える」という誤解を防いだ。

### 13. 雛形の目的の言語化と、実装の全数監査（2026-08-30）

#### ユーザーが確定させた「この雛形とは何か」

**目的**: 非エンジニアがサクサク安心して開発が進む仕組み。

**その手段**（5つ）: 並列ネイティブ／機械検証／進捗管理／放置と振り返りの仕組み／
非エンジニア向け説明ルール。

**構成**: 土台のみで構成する。

**制約**: **上物（プロジェクト固有）によって土台（雛形共通）が変わってはならない。
変えてよいのはユーザーの明示指示があるときだけで、AI の判断で変えてはいけない。**

この言語化の前、AI 側は雛形を「中身が空の器」という構造の話として説明していた。それは
手段の一部（点）であって、目的（線と面）ではない、というユーザーの指摘で修正された。

#### PR #35 を revert した理由

PR #35（技術構成の分離と `/project-init` の追加）は、`/project-init` が `AGENTS.md` を
書き換える設計だった。**土台の中心ファイルがプロジェクトごとに違う内容になる**ため、
上記の制約に正面から違反する。PR #36 で revert 済み（`main` は `425d082`）。

#### 監査の結果

サブエージェント3体に並列で全数調査させた（担当: 機械検証と並列／土台への上物混入／
進捗管理と振り返りと説明ルール）。**致命的と判定されたもの計27件。**

重要なのは「部品が無い」のではなく**「部品はあるが、どこからも呼ばれていない」**こと。
実際に配線されているのは `validatePlan`（CI から `pnpm run test` 経由）**1つだけ**。

**A. 部品はある。呼び出し元を作れば動く**

| 部品                                      | 場所              | 状態                                                       |
| ----------------------------------------- | ----------------- | ---------------------------------------------------------- |
| `countProgress`                           | `src/plan.ts:178` | 呼び出し元はテストのみ。**進捗を表示する手段が存在しない** |
| `nextItem`                                | `src/plan.ts:196` | 同上。`/checkin` は文章で「選べ」と書くだけ                |
| `canRunInParallel`                        | `src/plan.ts:208` | 同上。並列候補を列挙する手段が無い                         |
| `nextId`                                  | `src/plan.ts:215` | 同上。追記時の採番が AI の手作業                           |
| `completion-checker` / `failure-reviewer` | `.claude/agents/` | 定義はある。呼ぶきっかけが文章だけ                         |

**B. 部品そのものが存在しない**

- **複数 AI を並列で走らせる仕組み**。`git worktree` を作る箇所がリポジトリ全体でゼロ
  （唯一の言及は `/checkout` の `git worktree list` ＝消えるものの確認）。
  `/checkin` は「1セッションで扱うのは1項目だけ」と書いており、運用は完全に逐次。
  **「並列ネイティブ」を名乗る根拠が文章と未使用の関数しかない**
- **ゴール到達の検出**。`docs/neglected-log.md` は「ゴール到達時にだけ読む」と定めるが、
  到達を判定する関数もコマンドもフックも無い。**読まれる契機が永久に来ない可能性がある**
- **「前回の照合時点の済数」の記録場所**。`/checkin` の「10項目ごとに `/plan-verify`」は
  比較対象が存在せず、判定が AI の記憶頼み＝実質発火しない
- **非エンジニア保護の強制**。`.claude/hooks/` の4本はいずれも発話や質問文を検査しない

**C. 部品が浅く、作り直しが要る**

- `verify` の検証（`src/plan.ts:125`）は**空配列かどうかしか見ない**。
  `verify: ["テストが通る"]` が通る。`plan-init` が禁じている書き方を機械が拒めない
- 番号の不変性（`src/plan.ts:108-117`）は**単一スナップショットしか見ない**。
  前バージョンと比較しないため、**項目の削除・中身の全書き換え・全体の振り直しは
  すべて素通り**。実際に弾けるのは並び替えだけ
- `docs/plan.json` が無いとテストごとスキップされる（`src/plan.file.test.ts:42`）。
  **消しても緑のまま通る**

#### 最も重い構造的欠陥

**この雛形は「文章の指示は守られない」と自分で書いているのに、目的そのものを文章の指示で
実現しようとしている。** `.claude/hooks/format.sh:4` と `typecheck.sh:4` はこう書いている——
「`AGENTS.md` の記述は助言にすぎず守られない可能性がある。フックは決定論的に実行される」。
**機械に強制させているのは Prettier と tsc（＝上物の都合）だけ**で、非エンジニア保護には
同じ論理を適用していない。

そのうえ `AGENTS.md`（土台）が `@docs/handoff.md`（前プロジェクトの中身）を import し、
`docs/decisions.md`（同）を読むことを義務づけている。**土台が上物を強制的に呼び込む向きの
依存**になっており、コピー先の第1セッションは着手前に前プロジェクトの履歴を必ず読む。

#### 土台に上物が混入している箇所（主要なもの）

- `AGENTS.md` 171行のうち約50行（118〜166行の大半）が技術構成。毎セッション読まれる
- `.claude/settings.json`: SessionStart フックが `pnpm install` を実行。permissions も pnpm 決め打ち
- `.claude/hooks/format.sh`（Prettier 専用）/ `typecheck.sh`（tsc 専用）。
  **Node 以外のプロジェクトでは編集のたびに失敗する**
- `.claude/skills/checkout`・`plan-init`・`plan-verify` の検証コマンドが `pnpm run *` 決め打ち
- `docs/handoff.md` / `docs/decisions.md` / `README.md` は全文が雛形自身の話
- `README.md` に**実在しないスキル**（`/run-skill-generator`、`/verify`）の使い方が残っている
- `package.json` の `name` が `from-0`

#### 台帳が空である事実

`docs/failure-action-log.md` と `docs/neglected-log.md` は、導入（PR #27〜#30）以降
**エントリ0件**。書かなくても CI は緑、`/checkout` も通る。**書かないことに何の
ペナルティも無い構造が、実測で空のまま維持されている。**

### 14. 公式機能の下調べ（5手段の再設計に向けて、2026-08-31）

「13.」で「存在しない」「浅い」と判定した部品のうち、独自実装ではなく Claude Code 本体の
公式機能で代替できるものがないかを、再設計の議論に入る前に調べた（AGENTS.md「既存資産の
活用を最初に検討する。コードを書くのは最終手段」）。**この節は事実の記録であり、採用するか
どうかの判断はまだ確定していない。** 各手段の設計討議で使う根拠として、新しいファイルを
作らずここに追記する。出典は全て2026-08-31時点で一次情報（code.claude.com）を確認済み。

| 手段                            | 公式機能                                                                        | 区分                 | 根拠                                                                                                                                                                                                                           |
| ------------------------------- | ------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ①並列ネイティブ                 | git worktree 連携（`--worktree` フラグ、`EnterWorktree`/`ExitWorktree` ツール） | 公式                 | セッション・サブエージェントごとに隔離された作業ディレクトリを自動生成し、ファイル競合を機械的にブロックする。https://code.claude.com/docs/en/worktrees                                                                        |
| 同上                            | サブエージェント定義の `isolation: worktree`                                    | 公式                 | フロントマターに1行足すだけで、そのサブエージェントは専用 worktree で動く。https://code.claude.com/docs/en/worktrees#isolate-subagents-with-worktrees                                                                          |
| 同上                            | `Workflow` ツールの `parallel()` / `pipeline()`                                 | 公式                 | 複数サブエージェントを同時実行する「実行部分」そのもの。同時実行数はスクリプトが渡すリストの数で決まり、固定レーン数はない。https://code.claude.com/docs/en/workflows                                                          |
| 同上                            | Agent teams（実験的機能、既定オフ）の共有タスクリスト                           | 公式                 | 依存関係が解決したタスクだけが着手可能になり、ファイルロックで同時着手を防ぐ。`canRunInParallel` とほぼ同じ発想。https://code.claude.com/docs/en/agent-teams#assign-and-claim-tasks                                            |
| ②機械検証                       | `/goal` コマンド                                                                | 公式                 | 完了条件を宣言すると、実装した本人とは別の評価モデルが、会話に出た証拠だけを見て毎ターン「満たした／まだ／不可能」を判定する。https://code.claude.com/docs/en/goal                                                             |
| ③進捗管理                       | `TaskCreate`/`TaskList`/`TaskUpdate` 等の Task ツール                           | 公式（用途は限定的） | 依存関係付きの共有タスクリストを持つが、セッション（チーム）が生きている間だけの機構。複数セッション・複数日にまたがる `docs/plan.json` の代替にはならない。https://code.claude.com/docs/en/agent-teams#assign-and-claim-tasks |
| ④放置と振り返り／ゴール到達検出 | Routine（クラウド定期実行、`create_trigger` 系ツール）                          | 公式                 | マシンを閉じても動き続け、スケジュール実行や外部イベントをトリガーに結果を通知できる。`.claude/settings.json` に許可リストは既にあるが未使用。https://code.claude.com/docs/en/routines                                         |
| ⑤非エンジニア向け説明ルール     | `AskUserQuestion` ツール                                                        | 公式（強制力なし）   | 選択肢付きの構造化質問を出すツール自体は存在する。ただし「専門的な内容は必ずこの形式で聞け」と強制する公式ルールは無い                                                                                                         |

**示唆（まだ判断は確定していない）**:

- ①②④は独自実装を避けられる可能性が高い。特に④は `.claude/settings.json` が既に
  Routine 系ツールを許可しているのに一度も使われていない
- ③⑤は公式機能がカバーする範囲がこの雛形の要求と違う（③はセッション内限定、⑤は強制力が
  ない）ため、自作の必要が残る見込み

**前回セッションの誤り**: ①について「複数AIを並列で走らせる仕組み（git worktree を作る
箇所）がリポジトリ全体でゼロ」＝存在しないと判定していたが、これは雛形の中に無いだけで
Claude Code 本体には存在した。「部品が無い」と「雛形が使っていない」を混同しないこと。

### 15. 5手段：公式機能の採否（ユーザー方針「公式にあるならそれを使う、無いなら自作する」、2026-08-31）

「14.」の下調べを踏まえ、方針を適用して確定した。**手段単位ではなく、要求を分解した
部品単位で判定する。**1つの手段の中でも「公式で足りる部分」と「公式ではカバーされない
部分」が混在するため。

| 項目                                                  | 区分                   | 根拠 / 理由                                                                                                                                                                                                                                           |
| ----------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ①並列ネイティブ：実行部分（worktree作成・並列実行）   | 判断（公式機能を採用） | `--worktree`/`EnterWorktree`/`isolation: worktree`/`Workflow`の`parallel()`が実行そのものを提供する。自前でworktree管理コードを書く理由が無い。変更してよい条件: これらのツールが使えなくなった場合のみ自作を検討                                     |
| ①並列ネイティブ：並列可否の判定（`canRunInParallel`） | 判断（自作を継続）     | 依存関係・ファイル重複は`docs/plan.json`固有のデータ構造に依存する判定で、公式機能はこの構造を知らない。判定ロジックは自作のまま、実行だけ公式に渡す                                                                                                  |
| ②機械検証：実装中の自動継続実行                       | 判断（公式機能を採用） | `/goal`が完了条件に向けて自動でターンを継続し、実装者と別の評価モデルが毎ターン判定する。自前で同等のループ制御を書く理由が無い                                                                                                                       |
| ②機械検証：`/checkout`時点の独立完了判定              | 判断（自作を継続）     | `/goal`の評価役は同じ会話履歴を読む小型モデルで、実装者から完全に独立した「verify手順を最初からなぞる」検証にはならない。AGENTS.mdが要求する「statusもgit履歴も見ずに」を満たすには、独立した文脈を持つサブエージェント（`completion-checker`）が要る |
| ②機械検証：`verify`の中身の妥当性検査                 | 判断（自作を継続）     | 空配列チェックの強化は`docs/plan.json`固有のバリデーションで、公式機能の対象外                                                                                                                                                                        |
| ③進捗管理                                             | 判断（自作を継続）     | Task toolsはセッション/チームが生きている間だけの機構で、複数セッション・複数日にまたがる進捗管理を代替できない（14節）。`countProgress`等の呼び出し口を自作する                                                                                      |
| ④放置と振り返り：ゴール到達・見送り増加の検出と通知   | 判断（公式機能を採用） | Routineが定期実行と外部通知を提供する。`.claude/settings.json`は既に許可済み。自前でスケジューラを書く理由が無い                                                                                                                                      |
| ④放置と振り返り：見送り時に「書く」行動そのもの       | 判断（自作を継続）     | Routineは検出・通知はできるが、AIが失敗・見送り時に台帳へ書く習慣自体は強制できない。公式機能の対象外で、フックや指示の強化が要る                                                                                                                     |
| ⑤非エンジニア向け説明ルール：質問の出し方             | 判断（公式機能を採用） | `AskUserQuestion`ツールが選択肢付き構造化質問を提供し、既にこのセッションでも使われている                                                                                                                                                             |
| ⑤非エンジニア向け説明ルール：この形式を強制する仕組み | 判断（自作を継続）     | 「専門的な内容は必ずこの形式で聞け」を強制する公式ルールは無い。発話内容を検査するフック等の自作が要る                                                                                                                                                |

**次にやること（実装に落とすときの単位）**:

- 公式機能を採用する行は、既存のツール・コマンドを呼ぶ配線を書くだけで済む
- 自作を継続する行は、`docs/handoff.md`「次にやること」のA（既にある部品に呼び出し口を作る）
  ／B（存在しない部品を作る）の分類をこの表で置き換える

### 16. `from-0` 自身の `docs/plan.json` を作った（2026-08-31）

**方針転換**: これまで「`docs/plan.json` は雛形を使う側のプロジェクトが作るもので、
`from-0` 自身には作らない」としていた（「14.」「15.」時点の前提）。ユーザーの指示で
これを変更し、**「15.」の表をそのまま `docs/plan.json` の項目にした**。手作業で
1回作ってみることを優先し、作成そのものを仕組み化する（`/plan-init` を改善する等）のは
今回はやらない。**「大計画を作る」を今後仕組み化するときの、実例（ひな形）として使う。**

- 最初に「15.」の10行をそのまま12項目にしたが、ユーザーから「イシューの粒度まで
  分解しろ」という指示があり、27項目（うち1件統合で削除、実質26項目）まで分割し直した
- **分割した本人が粒度を判定しない**ため、独立したサブエージェントに `docs/plan.json` の
  品質レビューを依頼した。指摘（直すべき5件・直したほうがよい4件）は全て反映済み:
  - T007/T008 が `.claude/skills/checkout/SKILL.md` に既にある記述と重複していたため統合
  - フック新設項目（旧T022・T026）の `files` に `.claude/settings.json`（登録先）が
    抜けていたため追加
  - `nextId` 関連2項目の依存の向きが逆だったため修正
  - `nextItem` 用のCLI項目が無く無関係な項目に依存していたため新設（T027）し依存先を変更
  - `automation` の`ci`/`human`分類を、AIだけで確認できるものは`ci`に修正
- `pnpm run test` を実行し、`validatePlan` を通過することを確認済み
- **次に仕組み化する対象**: 「大計画をどう作るか」自体。今回手作業で作った過程
  （「15.」の表→イシュー粒度への分割→サブエージェントによる独立レビュー→反映）を
  ふまえて、`/plan-init` に反映するかどうかを次に検討する。**今回はまだ着手していない**

**26項目より先の分解はしないと再確認した**: ユーザーから「タスクの分解もするんだろ？
本番はそこまでやるんだろ？」という指摘があったが、`AGENTS.md`53行目
「セッションごとの実行計画はファイルに残さない」にすでに理由つきで明記されている
既存方針（根拠は「9.」節、Anthropic公式・spec-kit両方が実行計画を"Regenerable"＝
使い捨てと位置づけている一次情報）を提示して反論し、ユーザーが採用した。
各項目の `verify` が複数ステップの受け入れ条件（イシュー内のチェックリスト相当）に
なっている点は既に分解済みで、それより先の「実装手順そのものの書き出し」はしない、
という整理で決着。**同じ指摘が来たときはこの節を示せば足り、再検討は不要。**

### 17. `AGENTS.md` に「並列で進める」節を追加した（`docs/plan.json` T001、2026-08-31）

「15.」で「①並列ネイティブの実行部分は公式機能を採用する」と決めたが、**その公式機能の
呼び方がリポジトリのどこにも書かれていなかった**ため、決定が実行に繋がっていなかった。
判定（`canRunInParallel`）→ 隔離（worktree）→ 実行（`Workflow` の `parallel()`）の3手順として
`AGENTS.md` に書き下ろした。

| 項目                                                                  | 区分     | 根拠 / 理由                                                                                                                                                                                                                                                                        |
| --------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 並列可否の判定を `canRunInParallel` に一本化                          | **判断** | 「15.」で自作継続と確定済み。条件（依存なし＋ファイル重複なし）を文章で再掲すると二重管理になるため、条件の正本は関数、`AGENTS.md` はその呼び先だけを書く。変更してよい条件: 判定条件を `docs/plan.json` の外にも広げるとき                                                        |
| 隔離に `isolation: "worktree"` を使う                                 | **公式** | [worktrees](https://code.claude.com/docs/en/worktrees#isolate-subagents-with-worktrees)（「14.」で確認済み）。`Agent` ツール引数とサブエージェント定義のフロントマターの両方で指定でき、変更が無ければ自動で片付けられることは本セッションでツール契約の原文を確認した             |
| 実行に `Workflow` の `parallel()` を使う                              | **公式** | [workflows](https://code.claude.com/docs/en/workflows)。`parallel(thunks)` は渡した関数を同時実行して全部の完了を待ち、失敗した要素は `null` になる（本セッションで `workflow-authoring` リファレンス原文を確認）                                                                  |
| **`Workflow` はユーザーが明示的に頼んだときだけ呼ぶ**と明記           | **公式** | `Workflow` ツール自身の契約に、明示的な opt-in が無い限り呼んではいけないと規定がある。これを書かないと「並列にしてよい」と読んだ AI が勝手に大量のサブエージェントを起動する                                                                                                      |
| 例のスクリプトから `phases` / `phase` を省いた                        | **判断** | 進捗表示のグループ分けにしか使わず、非エンジニアが読む最小例では雑音になる。変更してよい条件: 段階が2つ以上ある例に差し替えるとき                                                                                                                                                  |
| `AGENTS.md` が 200 行の目安を超えた（171→223行。「20.」で再度増えた） | **判断** | 「1.」の 200 行目安に反する。T001 の deliverable が「`AGENTS.md` に書かれている状態」を要求しており、条件・隔離方法・呼び方の3点はどれも削ると挙動が変わる（「1.」の判定基準）。変更してよい条件: 他節に削減余地が見つかったとき、または分離しても遵守率が落ちないと確認できたとき |

**検証**: 節に載せた `Workflow` スクリプトは、実際に走らせる代わりに**スクリプト契約どおりに
解釈できるか**を機械的に確かめた（`export const meta` を外し本文を async 関数で包み、
`parallel`/`agent` をスタブに差し替えて `node --check` と実行）。`Workflow` の実行そのものは
ユーザーの明示的な依頼が要るため、この節では呼ばずに `docs/plan.json` の `T005`
（実際に2項目を並列で走らせる）へ回した。**例に書く項目の組も、実際に
`canRunInParallel` に通してから載せること**——初稿は `T002`/`T017` を例にしていたが、
両者は `files` に `package.json` を共有しており関数は `false` を返す（＝節が自分の
条件に違反した例を載せていた）。`completion-checker` の指摘で `T002`/`T009` に差し替えた。

### 18. Gate 抵触（ヒヤリハット）の検出設計（`docs/plan.json` T021、2026-08-31）

**解きたい問題**: `docs/neglected-log.md` は導入から**エントリ0件**（「13.」）。
書かなくても CI は緑で `/checkout` も通る。**書かないことに何のペナルティも無い構造**が
実測で空のまま維持されている。ここを機械で埋める。

**検出したいもの**: `docs/test-policy.md` の Gate に触れたが破らなかった場面（ヒヤリハット）。
具体的には、081〜100 の問題を見つけたが今回は直さないと判断したのに、Backlog Rule の
記録をしないまま作業を続けている状態。

| 決めごと                 | 内容                                                                                                                                                                                                                                                                             |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **いつ動かすか**         | `Stop` フック（ターンの終わり）。`/checkout` の中だけで見ると遅い——気づいた時点から締めまでの間に、何を見送ったかの記憶が薄れる                                                                                                                                                  |
| **何を見るか**           | ① 標準入力 JSON の `transcript_path`（そのセッションの会話記録 JSONL）② `git diff` で `docs/neglected-log.md` に今セッションの追記があるか                                                                                                                                       |
| **何を手がかりにするか** | 会話記録に現れる次の語: `FAILED_GATE` / 3桁の Gate ID（`081`〜`100`）/「見送」「後回し」「今回は直さない」「バックログ」「BACKLOG」「IGNORE」                                                                                                                                    |
| **どう判定するか**       | 上の手がかりが1つ以上あり、かつ `docs/neglected-log.md` に追記が無ければ「書き漏らし」とみなす                                                                                                                                                                                   |
| **何を出すか**           | **1セッションに1回だけ差し戻す**（`exit 2`）。`handoff-check.sh` と同じ作法。`exit 0` の標準エラー出力はモデルにもユーザーにも届かない（transcript を開かない限り見えない）ため警告として機能しない。毎ターン止めれば作業にならず、届かなければ意味が無い。その折衷が「1回だけ」 |
| **誤検出の扱い**         | 手がかりの語は日常会話にも出る（「後回し」など）。**誤検出は許容する。**書かない放置より、たまに余計に聞かれるほうが安い。ただし同じセッションで繰り返し出さない                                                                                                                 |

**登録先**: `.claude/settings.json` の `Stop`（`handoff-check.sh` の後ろ）。
`.claude/hooks/handoff-check.sh` からは `--check` 付きで呼び、手がかりの正本を2箇所に持たない。

**やらないこと**: 会話の意味を判定しようとしない。フックはシェルスクリプトで、
LLM を呼ばずに語の有無だけを見る。意味の判定を入れると、遅く・不安定になり・
「なぜ止まったか」を人が説明できなくなる。

### 19. 質問スタイル検査の設計（`docs/plan.json` T025、2026-08-31）

**解きたい問題**: `AGENTS.md` は「非エンジニア向けに説明する」「選択肢つきの YES/NO で聞く」
と指示しているが、指示は守られないことがある。守らせる仕組みが無い（「15.」⑤）。

**何をもって「非エンジニアに答えられない聞き方」とするか**。次のどれかに当てはまるものを言う。

| #   | 判定基準                     | 具体例（弾きたいもの）                                              |
| --- | ---------------------------- | ------------------------------------------------------------------- |
| 1   | **選択肢が無い**             | 「どうしますか？」「どちらがいいですか？」だけで、選ぶものが無い    |
| 2   | **専門用語が説明なしで出る** | 「lockfile を再生成しますか？」「rebase と merge どちらにしますか」 |
| 3   | **推奨が無い**               | 選択肢はあるが、どれを勧めるか・選ぶとどうなるかが書かれていない    |
| 4   | **判断材料が質問の外にある** | 「上のエラーを見て決めてください」                                  |

**機械で見られるのは 1 と 2 だけ**。3・4 は文意の判定が要るため、フックでは見ない `[曖昧]`。

| 決めごと                   | 内容                                                                                                                                                                      |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **いつ動かすか**           | `Stop` フック。`PreToolUse` で `AskUserQuestion` を捕まえる案もあるが、**それでは「ツールを使わずに素の文章で聞く」場合を素通りさせる**。弾きたいのはまさにそれ           |
| **何を見るか**             | 標準入力 JSON の `transcript_path` の、**最後のアシスタント発言だけ**。過去の発言まで見ると、既に答えた質問で毎回鳴る                                                     |
| **判定 1（選択肢が無い）** | 発言が `？` か `?` で終わる、または「ますか」「ください」を含む疑問文であり、かつ `AskUserQuestion` の使用も、行頭の番号つき選択肢（`1.` `2.` や `- `）も無い             |
| **判定 2（専門用語）**     | 用語リスト（`lockfile` `rebase` `worktree` `CI` `PR` `フック` `バリデーション` など）のいずれかを含み、かつその用語の説明（同じ発言内に「＝」「という」「つまり」）が無い |
| **何を出すか**             | **1セッションに1回だけ差し戻す**（`exit 2`）。理由は「18.」と同じ                                                                                                         |

**登録先**: `.claude/settings.json` の `Stop`。

**実データでの発火（2026-08-31）**: 作った本人の締めのメッセージが、このフックに差し戻された。
「`/goal clear` で目標を外していただくか、通知が届くようになった時点で改めて声をかけてください」
という一文で、**選択肢が無い**という判定（`choices`）。テスト用の作り物ではなく、実際の会話で
効いた最初の例。**指示（`AGENTS.md`「選択肢つきの YES/NO にする」）だけでは守られず、
フックで初めて止まった**ことの実証でもある。

**用語リストは育てる前提で作る。** 完全な一覧は最初から作れない。実際に非エンジニアが
詰まった語を追加していく。**リストの正本はフックのスクリプト内に置き、`AGENTS.md` には
書かない**（二重管理になるため）。

### 20. Routine（週次の進捗通知）を作った（`docs/plan.json` T018、2026-08-31）

| 項目                                            | 区分     | 根拠 / 理由                                                                                                                                                                                                                                        |
| ----------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 定期実行に Routine を使う                       | **公式** | 「15.」で採用を確定済み。[routines](https://code.claude.com/docs/en/routines)。`.claude/settings.json` は以前から許可していたのに一度も使われていなかった                                                                                          |
| 頻度は週1回（毎週月曜 00:00 UTC）               | **判断** | 1セッション1項目で進む速度に対し、日次は通知が空振りし続けて読まれなくなる。変更してよい条件: 複数 AI を並列で走らせて進みが速くなったとき                                                                                                         |
| 毎回まっさらなセッションで実行する              | **判断** | 前回の会話を引き継ぐと、報告が過去の文脈に引きずられる。読むだけの処理なので、毎回ゼロから見るほうが正確                                                                                                                                           |
| 報告内容を `plan:progress` の出力そのままにする | **判断** | 通知のために別の集計を書くと、画面と通知で数字が食い違う。数える場所を1つに保つ                                                                                                                                                                    |
| ゴール到達時だけ文言を変える                    | **判断** | 「④放置と振り返り」の要求は、ゴール到達を検出して振り返りに入らせること。毎回同じ文面だと、到達しても気づかれない                                                                                                                                  |
| **完了判定を判定役に委ねられなかった**          | **判断** | `completion-checker` は Routine の一覧を取得する手段を持たない（Read / Bash / Grep / Glob のみ）。作った本人が API の一覧で存在を確認した（`trig_01Am7Jmdx7g9wTC35KeX4eKN`、次回 2026-09-07）。**この1件だけは自己申告である**ことを記録として残す |

**止め方**: claude.ai の Routines 画面で無効化または削除する。リポジトリ側には何も残らない
（`.claude/settings.json` にも `docs/` にも Routine の定義は保存されない）。

### 21. 2項目を実際に同時実行した記録（`docs/plan.json` T005、2026-08-31）

「15.」で「①並列ネイティブの実行部分は公式機能に任せる」と決め、「17.」で手順を
`AGENTS.md` に書いた。**それを実際に一度走らせた記録。** 机上のままにしないための一周。

| 項目         | 内容                                                                                                                               |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| 走らせたもの | `Workflow` の `parallel()`。2つの `agent()` をそれぞれ `isolation: 'worktree'` で同時実行                                          |
| 対象         | `T028`（`.claude/skills/plan-init/SKILL.md`）と `T029`（`.claude/skills/handoff/SKILL.md`）。`canRunInParallel` が `true` を返す組 |
| 作業場所     | `.claude/worktrees/wf_439622fa-6fd-1` と `-2`。ツールが自動で作り、それぞれ別ブランチにコミットした                                |
| 所要時間     | 61秒（2体合計で約13万トークン）                                                                                                    |
| 取り込み     | `git merge --no-ff` を**1つずつ**。`git status` は空、`pnpm run format:check` も通過。**衝突なし**                                 |

**分かったこと**:

- **`AGENTS.md`「並列で進める」に書いた手順は、書いたとおりに動く。** 判定
  （`canRunInParallel`）→ 隔離（`isolation: 'worktree'`）→ 実行（`parallel()`）の3段が
  そのまま通った
- **触るファイルを重ねない判定が効いている。** 2体は同じリポジトリの別 worktree で
  同時にコミットしたが、マージは両方とも自動で通った
- **サブエージェントには「触ってよいファイル」を明示する必要がある。** 今回は
  プロンプトで1ファイルに限定した。`docs/plan.json` の `files` をそのまま渡せば足りる
- **worktree の中では `pnpm run format:check` がリポジトリ全体で落ちて見えることがある**
  `[曖昧]`。2体とも「`docs/decisions.md` が未整形」と報告したが、メイン側では通っていた。
  worktree に `node_modules` が無く、祖先をたどって解決した Prettier が別の設定を見た
  可能性がある。**サブエージェントには、全体ではなく変更したファイルだけを検査させるほうが
  誤報が少ない**

**やらなかったこと**: 3体以上での実行。2体で手順が通ることを確認するのが目的で、
同時実行数の上限（16件程度）の検証はしていない。

### 22. 人への確認をまとめて取った結果（`docs/plan.json` T014 / T019 / T026、2026-08-31）

`/plan-ask` の手順どおり、溜まった確認を**1件ずつ番号を振って、選択肢つきで**聞いた
（`AskUserQuestion`）。証拠は実物（実際の画面の出力と、フックが弾いた例・通した例）を提示した。

| 項目   | 聞いたこと                                 | 答え             | 反映                       |
| ------ | ------------------------------------------ | ---------------- | -------------------------- |
| `T014` | 進み具合の画面を見て、自分の言葉で言えるか | 言える           | `done`                     |
| `T019` | 手動実行した週次レポートの通知が届いたか   | **届いていない** | 原因を直して再実行（下記） |
| `T026` | 質問スタイル検査の線引きが妥当か           | これでよい       | `done`                     |

**`T019` で分かったこと（通知が届かなかった）**:

| 項目                 | 内容                                                                                                                                                      |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 何が起きたか         | Routine の実行そのものは成功していた（47秒で完了）。**届かなかったのは通知だけ**                                                                          |
| 原因の見立て         | 通知先がスマホへのプッシュ通知だけだった（`{push: true, email: false}`）`[曖昧]`                                                                          |
| 直し方               | プッシュとメールの両方に変更（`{push: true, email: true}`）                                                                                               |
| **なぜ作り直したか** | `update_trigger` に通知先を変える引数が無い。名前・スケジュール・本文は更新できるが、通知チャンネルだけは作成時にしか指定できない。**実行履歴は失われる** |

**再確認の結果（メールを足しても届かなかった）**: ユーザーの答えは「届かないしメールはいらない」。
2回目の実行も正常に完走した（97秒）が、通知は届かなかった。**メール送信は本人の希望で切り、
スマホ通知だけに戻した**（`{push: true, email: false}`。これも作り直しが要った）。

| 項目               | 内容                                                                                                                                   |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| 分かっていること   | Routine は毎回きちんと動いている（2回とも成功）。**動いていないのは通知の配送だけ**                                                    |
| 分かっていないこと | なぜ届かないか `[曖昧]`。リポジトリの外（claude.ai アプリ側の通知の受け取り設定）にある可能性が高いが、こちらからは見えない            |
| いま報告を読む方法 | claude.ai の Routines 画面で、実行ごとの報告を開いて読む                                                                               |
| `T019` の扱い      | **完了にしない**（`awaiting_human` のまま）。届いていないものを届いたことにしない。`T020` は `T019` を前提にするため、あわせて持ち越し |

**学び**: **「動いた」と「届いた」は別物。** 実行の成否は API で確認できるが、通知が人の手元に
届いたかは人に聞くしかない。`automation` が `human` の項目は、まさにこの差を埋めるためにある。
**今回はその仕組みが正しく働き、「動いているのに届いていない」という、機械だけでは永久に
気づけない状態を検出した。**

### 23. 週次レポートを取り下げ、`T019` / `T020` を決着させた（ユーザー判断、2026-08-31）

**ユーザーの言葉**: 「レポートは不要。で解決としろ」。

「22.」のとおり、Routine の実行は毎回成功するのに通知だけが届かなかった。原因はリポジトリの
外にあり、こちらからは追えない。ユーザーはレポート自体が不要と判断した。

| 項目                            | 内容                                                                                                                                                                                            |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Routine の扱い                  | **削除ではなく無効化**（`enabled: false`）。削除すると `T018`（Routine が実在する状態）が後から壊れる。必要になれば claude.ai の Routines 画面で1操作で戻せる                                   |
| `T019` `T020` の `status`       | `done`                                                                                                                                                                                          |
| **`done` の意味がここだけ違う** | この2件は **`verify` を満たしたのではなく、ユーザーが要件そのものを取り下げたことによる決着**。通知が届いた事実は無い。**後から読む人が「通知は届いたのだな」と誤解しないよう、ここに明記する** |
| 記録先                          | `docs/neglected-log.md` にも記録した（Gate `089` 主要な失敗を運用者が検知可能か）。定期的に進み具合を知らせる経路は現在ない                                                                     |

**残った検討事項** `[曖昧]`: `docs/plan.json` の `status` は `todo` / `awaiting_human` / `done` の
3つしかなく、「**要件そのものが取り下げられた**」を表せない。今回は `done` に寄せてこの節で
補ったが、項目数が増えると同じ誤解が起きる。`dropped` のような状態を足すかどうかは、
実際にもう一度同じことが起きてから決める（起きていない問題のために型を増やさない）。

### 24. `plan:*` を毎回ビルドしないようにした（2026-08-31）

`docs/neglected-log.md` に 081（主要操作が実用不能なほど遅くないか）として記録していた1件。
ユーザーが `/goal` の小さな目標として指定したため着手した（見送りのまま放置していたものを、
AI の判断で勝手に掘り返したのではない）。

| 項目           | 内容                                                                                                                                                                                            |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 何が遅かったか | `plan:progress` などの4コマンドが毎回 `pnpm run build`（tsc）を走らせていた。読むだけの操作に **1670ミリ秒**                                                                                    |
| なぜ困るか     | `/checkin` は起動のたびに `plan:progress` `plan:next` `plan:parallel` を叩く。1回あたりの差が積もる                                                                                             |
| 直し方         | `scripts/plan-cli.mjs` を入口にし、**`dist` が古いときだけ**ビルドする。結果 **約455ミリ秒**（3.6倍）                                                                                           |
| 古さの判定     | `src` の `.ts` と `tsconfig.build.json` のうち一番新しい更新時刻 対 呼び出す `dist` のファイルの更新時刻。**ハッシュではなく更新時刻**——目的はビルドの省略で、ビルドの正しさは tsc 側の責任     |
| 採らなかった案 | `node --experimental-strip-types` で `.ts` を直接実行する。**動かない**——このリポジトリは ESM で相対 import に `.js` を付ける規約のため、`./plan.js` が解決できず `ERR_MODULE_NOT_FOUND` になる |

**確認したこと**: 出力が変わらないこと、第1引数（別の計画ファイル）が通ること、
`src` を書き換えたら作り直されることを、いずれも実行して確かめた。

### 25. `T006` が「実行不能な確かめ方」を持っていた（2026-08-31）

`T006` の `verify` の2つ目は「実際に1項目で試し、`/goal` が正しく『満たした』と判定することを
確認する」。**これは AI のセッションからは原理的に実行できない。** `/goal` は人がターミナルに
入力するコマンドで、メインのセッションからもサブエージェントからも起動できない。

判定役（`completion-checker`）に最終判定を求めた結果は「**通らなかった**」。その理由の要点:

- 手順1（`AGENTS.md` に手順がある）は満たされている
- 手順2は「やってみたら失敗した」のではなく「**そもそも実行できない**」
- deliverable の文面（手順が書かれている状態）だけを見れば満たされているが、
  **`verify` は deliverable の言い換えではなく、より強い要求**。手順2を落として
  deliverable だけで合格にするのは、判定基準を後から緩めること

| 分かったこと                                  | 内容                                                                                                                                                                     |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`automation` の分類が実態と食い違っていた** | `T006` は `ci`（機械で確認できる）になっているが、手順2は人の操作を要する。**`human` が正しかった**                                                                      |
| なぜ間違えたか                                | `/plan-init` の分類基準は「その手順を機械が実行できるかどうか」。`/goal` は Claude Code の機能なので機械側だと考えたが、**起動できるのは人だけ**だった                   |
| 「未検証の懸念」が現実になった                | `AGENTS.md` に書いていた「`verify` を人間がたどれる手順として書き続けられるか `[曖昧]`」——**崩れ方は「書けなくなる」ではなく「書けているが AI には実行できない」だった** |
| 直せないこと                                  | `docs/plan.json` の既存項目は書き換え禁止のため、`automation` を `human` に直すことはできない。**この節が記録**                                                          |

**次に同じことを防ぐには**: `automation` を決めるとき、「機械が実行できるか」を
**そのコマンドを誰が入力するか**まで下りて確かめる。「Claude Code の機能だから機械側」は
成り立たない。`/goal` `/checkin` `/checkout` のようなスラッシュコマンドは、**人が入力する**。

### 26. `T006` を取り下げて閉じた（ユーザー判断、2026-09-01）

判定役の最終判定は「通らなかった」（「25.」）。手順2は AI からは原理的に実行できず、
人が `/goal` を1行入力しない限り観測できない。ユーザーは取り下げを選んだ。

| 項目               | 内容                                                                                                              |
| ------------------ | ----------------------------------------------------------------------------------------------------------------- |
| `T006` の `status` | `done`                                                                                                            |
| **`done` の意味**  | **`verify` を満たしたのではない。** 手順2（`/goal` が「満たした」と判定するのを見る）は**最後まで観測していない** |
| 満たしたもの       | 手順1（`AGENTS.md` に手順がある）のみ。deliverable の文面（手順が書かれている状態）は満たされている               |
| 記録先             | `docs/neglected-log.md` にも記録（Gate `099`）                                                                    |

**これで `verify` を満たさないまま `done` にした項目が3件になった**（`T019` `T020` `T006`）。
`docs/plan.json` の `status` に「要件が取り下げられた」を表す値が無いための妥協で、
28項目中3件、約1割。**「23.」では「もう一度同じことが起きてから決める」としたが、
起きた。次にこの雛形を使うプロジェクトでは、最初から `dropped` を持たせることを勧める。**

**この雛形自身の `docs/plan.json` は追記のみで書き換え禁止のため、いま型を変えることはしない。**
変えるなら `src/plan.ts` の `Status` に `dropped` を足し、`countProgress` の数え方
（完了に含めるか、母数から外すか）を決めるところから。

### 27. 全体照合とゴール到達時の振り返りを実施した（2026-09-01）

28項目すべてが `done` になったので、`/plan-verify`（全体照合）と、`docs/neglected-log.md` の
振り返りを回した。**このセッションでは27項目を一気に進めたため、10項目ごとの照合が
一度も回っていなかった。**

#### 全体照合（まっさらな照合役に委譲）

| 分類                               | 件数  | 内訳                                                                                               |
| ---------------------------------- | ----- | -------------------------------------------------------------------------------------------------- |
| できている                         | 18    | `verify` の手順を実際になぞって通った                                                              |
| 手順の一部が実行できない           | 6     | `T001` `T004` `T005` `T006` `T007` `T015`（`Workflow` やスラッシュコマンドを AI から起動できない） |
| 手順を全く実行できない             | 4     | `T014` `T018` `T019` `T020`（人の目、または claude.ai の画面が要る）                               |
| **書いてあることと違う動きをする** | **0** | —                                                                                                  |
| **頼んでいないものが増えている**   | **0** | —                                                                                                  |

**デグレは見つからなかった。** 特に懸念していた3点はいずれも健在:

- `plan:*` の4コマンドを `scripts/plan-cli.mjs` 経由に差し替えた後も、出力と第1引数の
  受け取りは同じ
- `validateVerifySteps` を後から足したが、`docs/plan.json` と `docs/plan.example.json`
  自身は弾かれていない（曖昧な書き方を入れたときだけ落ちることも再現確認）
- `handoff-check.sh` に台帳チェックを足したが、元々の引継ぎ差し戻し（変更あり→`exit 2`／
  変更なし→`exit 0`／1セッション1回）は健在。台帳の警告は後ろに付くだけ

**分かったこと**: **28項目中10項目（約36%）は、AI が単独では `verify` をなぞれない。**
内訳は「スラッシュコマンドを起動できない」6件と「人の目が要る」4件。
`automation` は `human` が4件しか付いていないので、**分類が実態より少なく見積もられている**
（「25.」で `T006` について指摘した問題が、`T001` `T004` `T005` `T007` `T015` にも当てはまる）。

#### 放置台帳の振り返り（4件）

| 触れたGate | 対象箇所             | いま確認した結果                                                                                                                                                        |
| ---------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `096`      | 追加した2本のフック  | **判断は今も成立**。「後回し」の一語だけで発火することを再現確認。1セッション1回の制限も健在。誤検出を許容する設計どおり                                                |
| `081`      | `plan:*` の4コマンド | **解消済み**。417〜425ミリ秒（元は約1600ミリ秒）                                                                                                                        |
| `089`      | 週次 Routine         | **判断は今も成立**。Routine は無効（ユーザーが一時停止した状態）。押して知らせる経路は無く、`pnpm run plan:progress` で引く形だけが残る。`AGENTS.md` の記述も実態と一致 |
| `099`      | `T006`               | **判断は今も成立**。`verify` を満たしていないことが `docs/decisions.md` 2箇所と引継ぎに明記されている                                                                   |

**リリース前の判断**: `docs/test-policy.md` の Release Gate（001〜080 ALL YES）を阻害する
問題は見つからなかった。残る4件はいずれも 081〜100 の範囲で、記録どおり実運用上の
支障になっていない。

### 28. `automation` の分類ずれを6件記録した（2026-09-02）

「27.」で「28項目中10項目（約36%）は AI が単独では `verify` をなぞれない」と判明した一方、
`automation` が `human` の項目は `T014` `T019` `T020` `T026` の4件しか無かった。実態と
食い違っている項目を洗い出し、正しい分類をここに記録する。

**`docs/plan.json` の既存項目は書き換え禁止**（AGENTS.md「全体計画」節）のため、以下の6件は
`automation: "ci"` のまま変更しない。**正しくは `human`**であることをこの節で補う（「25.」で
`T006` について行ったのと同じ扱い）。

| id     | `verify` が要求する操作                                                                    | なぜ機械だけでは完結しないか                                                             |
| ------ | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| `T001` | 「書かれた手順どおりに `Workflow` を呼び出せることを目視で確認する」                       | `Workflow` はユーザーが明示的に頼んだときだけ呼べる。AI が自分の判断だけでは起動できない |
| `T004` | 「並列候補がある状態で `/checkin` を実行し…」                                              | `/checkin` はスラッシュコマンドで、人がターミナルに入力する                              |
| `T005` | 「依存もファイル重複も無い2項目を `parallel()` で同時に実行する」                          | `T001` と同じく `Workflow` の起動が要る                                                  |
| `T007` | 「`docs/plan.json` の適当な項目を1つ完了させた状態で `/checkout` を実行する」              | `/checkout` はスラッシュコマンドで、人がターミナルに入力する                             |
| `T015` | 「依存を満たした `todo` 項目が複数ある `docs/plan.json` で `/checkin` を実行する」         | 同上                                                                                     |
| `T018` | 「`claude.ai/code/routines` または `/schedule` で Routine が作成されていることを確認する」 | claude.ai の画面を人が開いて見る必要がある。機械のログだけでは確認できない               |

**`T018` は「27.」の6件（`T001` `T004` `T005` `T006` `T007` `T015`）には含まれていなかった。**
「27.」は `T018` を「手順を全く実行できない」4件（`T014` `T018` `T019` `T020`）の側に分類していたが、
`automation` が実際に `human` になっているのはそのうち `T014` `T019` `T020` だけで、`T018` だけ
`ci` のまま取り残されていた。合わせて6件がこの節の対象。

**次に同じ見落としを防ぐには**: `/plan-init` で新規項目を起票するとき、`verify` の各手順に
「これを実行するのは誰か（AI のセッション／人がターミナルに入力／人が画面を見る）」を
1手順ずつ書き出してから `automation` を決める。「25.」で立てた基準（「そのコマンドを誰が
入力するか」まで下りる）を、`T006` 単体ではなく `verify` の全手順に適用する。

### 29. 雛形から実プロジェクトへ移行した（ユーザー指示、2026-09-02）

ユーザーが実プロジェクトの要件（44項目）を提示したため、引継ぎの選択肢3を実行した。
このセッションで作ったのは**計画だけ**で、コードは1行も書いていない（ユーザー指示
「受け入れ基準もきっちり決めろ・計画承認後は待機」）。

#### T 番号の衝突をどう避けたか

| 項目                 | 内容                                                                                                                                                         |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 問題                 | `docs/plan.json` は雛形自身の28項目で埋まっており、既存項目は書き換え禁止。新プロジェクトの項目を追記すると `T029` から始まり、番号だけでは2つの計画が混ざる |
| 採った方法           | 雛形の計画を `docs/plan.from-0.json` に `git mv` し、`docs/plan.json` を新しい計画（`T001`〜`T043`）として作り直した                                         |
| なぜ改名が許されるか | 「書き換え禁止」は**項目の本文**についての規約。ファイルごと別名で保存し、中身を1文字も変えていないので、過去の PR・台帳からの参照は改名後のファイルで辿れる |
| 参照の読み替え       | **`docs/decisions.md` の「28.」までに出てくる T 番号は、すべて `docs/plan.from-0.json` を指す。** 「29.」以降は `docs/plan.json`（新プロジェクト）を指す     |
| 検証                 | `src/plan.file.test.ts` は `docs/plan.json` が存在するときだけ検証する作りなので、差し替え後も `pnpm run test` が新しい計画を検証する（46件通過を確認済み）  |

**採らなかった案**: 新プロジェクトの項目を既存 `docs/plan.json` の末尾に `T029` から追記する。
`plan:progress` の「当初計画28件」の分母に雛形の項目が混ざったままになり、
新プロジェクトの進み具合が読めなくなるため却下した。

#### 技術選定（調査した一次情報）

**方針は「Mercur を改造して授業用経済圏にする」**。ユーザーの要件44番の構成をそのまま採る。

| 決めたこと                                           | 区分     | 根拠                                                                                                                                                                                                                                                     |
| ---------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 市場の土台に Mercur を使う                           | **判断** | MIT。マルチベンダー市場が主目的で、Storefront・Vendor Panel・Admin の3画面が揃う。[github.com/mercurjs/mercur](https://github.com/mercurjs/mercur)                                                                                                       |
| Mercur は `apps/` に取り込む（別リポジトリにしない） | **判断** | ユーザーが選択。計画の「触るファイル」判定・CI・引継ぎが1つのリポジトリで完結する                                                                                                                                                                        |
| バックエンドは Medusa `2.18.0` 系                    | **公式** | `apps/api/package.json` の `@medusajs/*` が全て `2.18.0`、`engines.node >=20`。[raw.githubusercontent.com](https://raw.githubusercontent.com/mercurjs/mercur/main/apps/api/package.json)                                                                 |
| 商品情報の多言語は Translation Module                | **公式** | `@medusajs/medusa/translation`、Medusa v2.12.3+。Product・Category・Collection・Variant 等を翻訳できる。[docs.medusajs.com](https://docs.medusajs.com/resources/commerce-modules/translation)                                                            |
| ロケール指定は `locale` クエリか `x-medusa-locale`   | **公式** | Store API の全ルートが両方に対応し、訳が無ければ原文にフォールバックする。[docs.medusajs.com](https://docs.medusajs.com/resources/storefront-development/localization)                                                                                   |
| **画面文字列の翻訳は自前で用意する**                 | **判断** | Mercur の Storefront は言語・通貨・配送地域が結合しており、分離は未実装の Issue #1363（2026-08-07 起票、open、PR 無し）。[github.com](https://github.com/mercurjs/mercur/issues/1363)                                                                    |
| テストは SurveyJS Form Library                       | **公式** | MIT。[github.com](https://github.com/surveyjs/survey-library/blob/master/LICENSE)。Survey Creator は別ライセンスのため使わない                                                                                                                           |
| **採点はサーバー側で行う**                           | **判断** | 得点が MP になるため、`correctAnswer` をブラウザに送ると資金を自作できてしまう。受け入れ基準 E4 で「問題取得の応答に正解が含まれない」ことを検査する                                                                                                     |
| MP 口座の実装方式は**未定**（`T003` で実機比較）     | **判断** | Store Credit Module は `@medusajs/loyalty-plugin` にあり Medusa v2.14.0+ が必要。口座・入出金・履歴はあるが、**公式ドキュメントに有効期限の記載が無い** `[曖昧]`。[docs.medusajs.com](https://docs.medusajs.com/resources/commerce-modules/store-credit) |
| 認証方式は**未定**（`T004` で実機確認）              | **判断** | Market ID を `emailpass` の識別子に渡せるか（メール形式の検査があるか）は未確認 `[曖昧]`。[docs.medusajs.com](https://docs.medusajs.com/resources/commerce-modules/auth/auth-providers/emailpass)                                                        |
| Chart.js / Graphology は MVP に入れない              | **判断** | Mercur 標準の統計で足りるか未確認のため、足りないと分かってから入れる。循環取引検知も SQL で数える方式を先に試す                                                                                                                                         |

#### 実行環境で確かめたこと（2026-09-02、このコンテナ）

- Node 22.22 / pnpm 10.33 / bun あり / PostgreSQL 16 のサーバー実体あり（`/usr/lib/postgresql/16/bin/`、PATH 外）/ redis-server 7 あり
- **Docker デーモンは動いていない**（CLI だけ）。`docker-compose` 前提の起動手順は使えないので、
  `T001` ではローカルクラスタを直接起動する方式にする
- CI は `check` ジョブ1本でサービス無し。`T002` で postgres を足すが、**ジョブ名は変えない**
  （Ruleset の必須チェックが外れて PR がマージ不能になる）

#### 受け入れ基準を A〜K で先に固定した理由

ユーザー指示「受け入れ基準もきっちり決めろ」。`docs/requirements.md` 第2部に、A（匿名認証）から
K（非機能）まで **1つずつ確かめられる文**として書いた。`docs/plan.json` の43項目は、それぞれ
どの基準を満たすためのものかが `deliverable` から辿れる。**「MVP が終わった」の判定は
`T043` で A1〜K3 を1つずつ実物で確かめる**。個々の項目の `done` の積み上げでは判定しない。

#### この計画の `[曖昧]`

- **`automation` を `ci` にした項目のうち、E2E に近いものは実際には人の操作が要るかもしれない。**
  「28.」で学んだとおり、`verify` の手順を誰が実行するかを、着手時にもう一度確かめること
- 授業が PC 前提かスマホ併用かが未確認。受け入れ基準 J3（幅375px）の要否がこれで変わる
- ロック時間・相互取引率のしきい値などの数値は仮置き。管理者が変えられる作りにして逃げている
- **43項目という数は、1項目1セッションで終わる想定。** 実際にやってみて大きすぎたら、
  末尾に分割した項目を追記する（既存項目は書き換えない）

### 30. 個人情報を「列で禁じる」から「中身で禁じる」に変えた（ユーザー判断、2026-09-02）

`T001` で Mercur を実際に起動し、マイグレーションを流したところ、**個人情報を入れる列が
47個できた**。`customer.email` `seller.email` `member.email` `order.email` のほか、
住所・氏名・電話が `cart_address` `customer_address` `fulfillment_address` `order_address`
`seller_address` `stock_location_address` に並ぶ。`member.email` と `seller.email` は
`NOT NULL` で、空にすらできない。

これらは Medusa 本体が使う列なので消せない。**当初の受け入れ基準 A3「そういう列が1つも
無いこと」は、この土台の上では原理的に満たせない。**

| 項目               | 内容                                                                                                                                                            |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ユーザーの判断     | 「欄は残し、中身を空にする」                                                                                                                                    |
| なぜ成り立つか     | 要件35の文言は「個人情報を**保存しません**」であって「列を持たない」ではない。要件37の狙いは「DB が漏れても本人が特定できない」こと。中身が空なら満たせる       |
| 直した受け入れ基準 | A3 を2段構えにした。(a) **自分で作ったテーブル**は列の存在自体を禁じる (b) **引き継いだテーブル**は中身が空か `MKT-XXXX-XXXX@anon.invalid` だけであることを見る |
| 検査の実装         | `scripts/check-no-personal-data.mjs`。CI の `check` ジョブで毎回走る                                                                                            |

**実際に確かめたこと（3通りとも実行）**:

| 試したこと                                              | 結果                                      |
| ------------------------------------------------------- | ----------------------------------------- |
| そのまま実行                                            | 違反0件（1916列を検査）                   |
| 自作テーブルに `email` 列を作る                         | `mp_test_own.email` を指摘して終了コード1 |
| `customer.email` に `tanaka@example.com` を入れる       | 値ごと指摘して終了コード1                 |
| `customer.email` を `MKT-7F4K-29QX@anon.invalid` にする | 違反0件で通る                             |

**この判断の弱点** `[曖昧]`: 引き継ぎテーブルは「中身を見る」検査なので、**検査した瞬間に
入っていなければ通ってしまう**。列がある限り、書き込む経路を塞ぐのはコード側の責任で、
機械で完全には守れない。だから `T008` 以降で「生徒がこれらの項目を入力できる画面・API を
作らない」ことを併せて守る必要がある。**`INHERITED_TABLE_PREFIXES` に新しいテーブルを
足すと検査が緩む。足してはいけない。**

### 31. Market ID は標準の認証にそのまま渡せた（`docs/plan.json` T004、2026-09-02）

**結論: 独自の認証プロバイダは要らない。標準の `emailpass` をそのまま使う。**

`emailpass` は識別子を `email` という名前で受け取るが、**中身がメールアドレスの形かどうかを
検査していない**。実装（`@medusajs/auth-emailpass@2.18.0` の `services/emailpass.js`）が
見ているのは `isString(email)` だけ。

**実際に API を叩いて確かめた**（再現手順: `pnpm run services` のあと API を起動し、
`node scripts/probe-auth.mjs`）:

| 試したこと                                                  | 応答                                                                |
| ----------------------------------------------------------- | ------------------------------------------------------------------- |
| `POST /auth/customer/emailpass/register` に `MKT-4PJZ-JGN2` | **200**。トークンが発行された                                       |
| 同じ Market ID とパスワードでログイン                       | **200**。トークンが発行された                                       |
| 存在しない `MKT-ZZZZ-ZZZZ` でログイン                       | 401 `{"type":"unauthorized","message":"Invalid email or password"}` |
| 正しい ID + まちがったパスワード                            | 401 **まったく同じ本文**                                            |

| 分かったこと                               | 内容                                                                                                                |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| **受け入れ基準 A2 は標準の挙動で満たせる** | 「ID が無い」と「パスワードが違う」で応答が同一。ID の存在が漏れない                                                |
| **登録しても `customer` 行はできない**     | 実際に確認した結果、`customer` は0件のまま。`provider_identity.entity_id` に Market ID が入るだけで、`email` 列は空 |
| **個人情報の検査も通ったまま**             | アカウントを2件作ったあとに再実行して違反0件                                                                        |

**残る作業**: 画面に出す文言は「Invalid email or password」のままでは要件に合わない
（メールを使わないため）。表示は翻訳辞書側で差し替える（`T009` `T032`）。

### 32. Medusa の Translation Module は使わない（`docs/plan.json` T005、2026-09-02）

**結論: 商品名・商品説明の多言語（受け入れ基準 I3）は自前のテーブルで持つ。**
Medusa の Translation Module は、この組み合わせ（Medusa 2.18.0 + `@mercurjs/core` 2.3.3）
では**動かせなかった**。

**試したことと結果**（すべて実行して確かめた）:

| 試したこと                                                               | 結果                                                                              |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------- |
| `medusa-config.ts` の `modules` に `@medusajs/medusa/translation` を足す | マイグレーションは成功。`translation` と `translation_settings` の表ができた      |
| `featureFlags: { translation: true }` を足して起動                       | 起動ログに `Using flag MEDUSA_FF_TRANSLATION from project config with value true` |
| `POST /admin/translations`                                               | **404**（実装を読むとこの経路は `GET` しか持たない）                              |
| `POST /admin/translations/batch`（実装から見つけた正しい経路）           | **404**                                                                           |
| `GET /admin/translations` / `/settings` / `/statistics`                  | **404**                                                                           |
| `GET /admin/translations/entities`                                       | **400**（引数不足のエラー＝経路は存在する）                                       |
| 環境変数 `MEDUSA_FF_TRANSLATION=true` で起動し直す                       | 変化なし。404 のまま                                                              |
| `translation` 表に英訳の行を **SQL で直接入れて** 商品を取得             | `x-medusa-locale: en` を付けても**原文のまま**。`?locale=en` は **400**           |

**分かったこと**: 404 になる経路にはどれも
`defineFileConfig({ isDisabled: () => !FeatureFlag.isFeatureEnabled('translation') })` が
付いており、唯一通った `entities` にはそれが無い。**フラグは読み込まれているのに、
経路を組み立てる時点では「無効」と判定されている。** なぜそうなるかは追い切れていない `[曖昧]`。

**なぜ深追いしないか**: 読み出し側（翻訳を入れても原文が返る）まで動かないため、仮に
書き込み経路を通せても I3 は満たせない。Medusa 自身もこの機能を experimental として
出している。土台の実験機能の不具合を追うより、自前で持つほうが確実で速い。

**代わりの方針**（`T036` で実装する）:

- 商品ごとに「原文 + 各言語の訳」を持つ**自前のテーブル**を作る
  （`reference_id` / `locale_code` / `title` / `description`）
- 表示するときは、閲覧者の言語の訳があればそれを、無ければ原文を返す
- **この表は自作テーブルなので、個人情報の列を持ってはいけない**
  （`scripts/check-no-personal-data.mjs` の (a) 側で機械的に守られる）

**この判断で変わること**: `docs/decisions.md`「29.」の表にある
「商品情報の多言語は Translation Module」は**取り消し**。画面文字列の翻訳を自前で持つ
方針（同表）は変わらない。結果として**多言語は商品情報も画面文字列も自前**になる。

### 33. MP 口座は自前で作る（`docs/plan.json` T003、2026-09-02）

**結論: Medusa の Store Credit は流用しない。MP の口座と取引履歴は自前のモジュールで作る。**

`@medusajs/loyalty-plugin@2.18.0`（MIT）を実際に入れて動かした
（再現手順: プラグインを入れ、`medusa-config.ts` の `modules` に
`@medusajs/loyalty-plugin/.medusa/server/src/modules/store-credit` を足して
`node scripts/run-api.mjs exec ./src/scripts/probe-store-credit.ts`）。

**できたこと**:

| 試したこと                   | 結果                                              |
| ---------------------------- | ------------------------------------------------- |
| 口座を作る                   | できた（`sc_acc_...`）                            |
| 100,000 を入金、2,500 を出金 | できた。取引2件、合計 **97,500**                  |
| 取引が持つ欄                 | `type` `reference` `reference_id` `note` `amount` |

**できなかったこと（これが決め手）**:

| 要件                                      | Store Credit での結果                                                                 |
| ----------------------------------------- | ------------------------------------------------------------------------------------- |
| **ボーナスの有効期限**（受け入れ基準 E2） | `expires_at` を渡しても**欄が無く、黙って捨てられる**。口座にも取引にも日付の欄が無い |
| **企業から企業への移転**（同 D1）         | 口座は `customer_id` に結びつく。この仕組みの企業は `seller` であって顧客ではない     |
| **企業ごとに口座を分ける**                | `customer_id` と `currency_code` の組で**1つしか作れない**（2つ目でエラー）           |
| **取引の追記のみ・逆仕訳**（同 K3）       | 追記のみを強制する仕組みは無い                                                        |

**採らなかった案**: 有効期限を `metadata`（json）に入れて逃げる。期限切れを毎日拾う処理と
「ボーナスから先に使う」処理は、期限で絞り込む検索が要る。索引の張れない json に入れると
60社ぶんの検索が現実的でなくなる。**曲げて使うより自前のほうが単純**。

**自前で作るもの**（`T014` `T016` で実装）:

- 企業ごとの口座（通常残高・ボーナス残高）
- **追記しかできない取引履歴**（更新も削除も禁じ、取り消しは逆仕訳を足す）
- ボーナスは `expires_at` を持ち、支払いは**ボーナスから先に**引く
- 「残高＝履歴の合計」を毎回確かめられるコマンド（`T017`）

**この判断で変わること**: `docs/decisions.md`「29.」の表の
「MP 口座の実装方式は未定（`T003` で実機比較）」を、**自前で作る**に確定した。

### 34. 広告経由の購入を「広告が出ている商品の購入」で数える（`docs/plan.json` T024・T025、2026-09-02）

**結論: MVP では「押してから買ったか」を追わない。広告が出ている商品が買われたら、
その広告の成果として数える。**

要件13は広告の効果として購入数と ROAS を求めている。厳密に数えるなら、
広告を押した人を覚えておき、そのあと同じ商品を買ったかを見ることになる。

| 項目           | 内容                                                                                                                         |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 厳密にやる方法 | 押した記録を残し、購入時に「その人が押していたか」を照合する                                                                 |
| なぜやらないか | 押した人を覚えるには**誰が押したか**を持つ必要がある。この仕組みは生徒を識別しない（要件35・37）ので、企業単位でしか追えない |
| どう数えるか   | 購入した商品にいま出ている広告があれば、その広告の成果（`conversion`）として1件数える                                        |
| ずれる場面     | 広告を見ずに市場一覧から直接買った場合も、広告の成果に数えてしまう                                                           |

**この割り切りが授業の狙いを壊さない理由**: 生徒が知りたいのは「広告費を出したら
売上がどう変わったか」で、そこは正しく出る。広告を出していない期間の購入は
成果に数えないため、出す前と出した後の比較はできる。

**厳密にしたくなったら**: 押した記録に企業の id を残し、購入時に照合する。
ただし「誰が押したか」は持たない（生徒を識別しない）ため、**企業単位でしか
正確にできない**。要件35を曲げてまで精度を上げる価値があるかは、実際に授業で
使ってから決める。

### 35. 管理画面のログインは `develop` でしか通らない（`docs/plan.json` T029、2026-09-02）

**結論: 管理画面（`/dashboard`）を人が開いて確かめるときは、バックエンドを
`pnpm run api:dev`（`medusa develop`）で動かす。`pnpm run api:start` では
ログインできない。**

T029 の画面を実際に開いて確かめようとして、ログインが通らずに気づいた。
検査で分かったこと（いずれも `http://localhost:9000` に対して実行）:

| 起動のしかた                    | `POST /auth/user/emailpass` | `POST /auth/session` の `Set-Cookie` | 画面のログイン |
| ------------------------------- | --------------------------- | ------------------------------------ | -------------- |
| `pnpm run api:start`（`start`） | 200（合鍵は返る）           | **無し**                             | 通らない       |
| `pnpm run api:dev`（`develop`） | 200                         | `connect.sid=...; HttpOnly`          | 通る           |

**理由の見立て `[曖昧]`**: `medusa start` は `NODE_ENV=production` で動く。
この状態の session cookie は「暗号化された通信でのみ送る」設定になり、
`http://` では**そもそも発行されない**。TLS を張れば `start` でも通るはずだが、
この実行環境では確かめていない。

**検査スクリプトへの影響は無い。** `scripts/check-*.mjs` は cookie ではなく
合鍵（`Authorization: Bearer`）で呼ぶので、`start` のままで通る。
影響を受けるのは**ブラウザで管理画面を開くときだけ**。

### 36. 画面文字列の翻訳は JSON の辞書で持ち、過不足を機械で調べる（`docs/plan.json` T032、2026-09-02）

**結論: 6言語の辞書を `apps/storefront/src/i18n/*.json` に置き、
`scripts/check-i18n-keys.mjs` で過不足を機械的に見つける。**

「32.」で Medusa の Translation Module を使わないと決めた続き。画面文字列については
最初から自前の方針だったが、**どう間違いを見つけるか**をここで決めた。

| 決めたこと           | 内容                                                                            |
| -------------------- | ------------------------------------------------------------------------------- |
| 置き場所             | `apps/storefront/src/i18n/<ロケール>.json`。言語の一覧は `locales.ts` が正本    |
| 突き合わせの基準     | `ja-JP`。**基準に無いキーは問題にしない**（翻訳側の余分なキーは画面を壊さない） |
| 見つけるもの         | 訳が無い／空になっている／日本語のまま／差し込み（`{{count}}`）が違う           |
| 同じ文字列でよいもの | `settings.ts` の `ALLOW_SAME_AS_BASE`（サービス名・`MP`・「市場」）             |
| 判定の中身を書く場所 | `keys.ts` の純関数。**検査スクリプトと単体テストが同じ関数を読む**              |

**「日本語のまま」を問題として出すことにした理由**: キーが揃っているかだけを見ると、
値を日本語のままコピーした辞書が検査を通ってしまう。実際、この検査を入れた直後に
簡体字の `market.count` が日本語と同じ文字列のままなのを見つけた（`件` は中国語でも
正しい助数詞だが、区別が付かないので中国語らしい表現に直した）。

**未確認**: `en` 以外の5言語の訳は**このセッションで機械が書いたもので、
母語話者が読んでいない** `[曖昧]`。受け入れ基準 I4（日本語を隠して一人で完走できるか）は
人が確かめる項目として `T041` に分けてある。そこで直すこと。

**差し込みの検査を入れた理由**: 翻訳のときに `{{count}}` を消しても画面は壊れず、
**その言語だけ数字が出ない**状態になる。動かして見ても気づきにくいので機械で見る。

### 37. 企業の名乗りは合鍵から決める（`docs/plan.json` T044、2026-09-02）

**結論: 企業として行う操作の Market ID は、ログイン時に発行された合鍵（JWT）から読む。
本文やクエリに書かれた `market_id` は読まない。**

`T032` を終えて次に進む前に `docs/test-policy.md` の重大度ゲートをたどったところ、
**P1「セキュリティ基本」の 033・034・036 が NO** だった。ゲートの決まり
（最初に NO になった項目だけを直し、下位を先回りしない）に従い、多言語の作業を止めて
ここを直した。

**再現した内容**（実際に実行した）:

```
POST /store/purchases  { market_id: <他社の Market ID>, listing_id: ... }
→ 201。ログインしていないのに、他社の残高で商品を買えた
```

同じ形で `POST /store/listings`（他社名義の出品）、`GET /store/dashboard`・
`GET /store/transactions`（他社の経営数字の閲覧）、`POST /store/quizzes/*/submit`、
`POST /store/ads`、`GET /store/ads/*/metrics`（他社の広告の効果）も通っていた。

| 直したこと                                  | 内容                                                                  |
| ------------------------------------------- | --------------------------------------------------------------------- |
| `apps/api/src/api/middlewares.ts`           | 上記の経路に `requireOrganization` を掛け、合鍵の署名を確かめる       |
| `apps/api/src/modules/market-auth/token.ts` | 合鍵から Market ID を読む純関数。**運営者の合鍵は企業として扱わない** |
| 各経路                                      | `body.market_id` を読むのをやめ、`marketIdOf(req)` だけを見る         |
| `GET /store/ads/*/metrics`                  | 自社の枠でなければ 403                                                |

**公開のまま残したもの**: アカウント作成（まだ誰でもない）、パスワード再設定
（Recovery Code が合鍵の代わり）、市場一覧・商品詳細・ランキング・Featured 枠の表示
（誰が見てもよい）、広告のクリック記録（誰が押したかは持たない、「34.」）。

**この判断で変わること**: 検査スクリプトは全て**先にログインして合鍵を渡す**形に直した。
生徒の画面（`apps/storefront`）も、購入や出品を作るときは合鍵を持つ必要がある。

**なぜ最初から気づかなかったか**: 検査を「正しい使い方をしたときに正しく動くか」だけで
書いていた。**「その操作をしてよい相手か」を確かめる検査が1本も無かった。**
いまは各検査に「ログインしていない」「別の企業の合鍵」の2つを入れてある。

### 38. 商品名と説明の翻訳は自前の表で持つ（`docs/plan.json` T036、2026-09-02）

**結論: `listing_translation` という自前の表に「商品ごと・言語ごとの訳」を持ち、
閲覧者の言語の訳があればそれを、無ければ原文を出す。**

「32.」で Medusa の Translation Module を使わないと決めた続き。実装で決めたこと:

| 決めたこと         | 内容                                                                |
| ------------------ | ------------------------------------------------------------------- |
| 原文の置き場所     | `listing`。訳の表には**複製しない**（片方だけ変わって食い違うため） |
| 訳の必須／任意     | **任意**。必須にすると6言語ぶん書けない生徒は商品を出せなくなる     |
| 片方だけ訳したとき | 空の側は原文を使う（途中まで訳した労力を無駄にしない）              |
| 訳が無いとき       | 原文を出す。**キーや空文字は出さない**                              |
| 言語の指定         | ヘッダ `x-locale` とクエリ `?locale=` の両方                        |
| 誰が訳したか       | **持たない。** 企業の単位までで十分（受け入れ基準 A3）              |

**落とし穴を1つ踏んだ**: `/store/*` の経路では **Medusa が `req.query` を作り直しており、
こちらで足した項目（`locale`）が消える**。ヘッダは通るのにクエリだけ無視され、
原文が返り続けた。`req.url` の文字列から直接読むことで回避した。
`req.query` を使っている既存の経路（ダッシュボード等）は合鍵から企業を決める形に
変えてあるので影響を受けない。

**一覧では訳をまとめて1回で読む。** 1件ずつ読むと商品の数だけ問い合わせが出る。
60人が同時に市場を開く前提なので、ここは詰めてある。

### 39. 同時に来た購入で残高と在庫が狂わないようにする（`docs/plan.json` T040、2026-09-02）

**結論: 在庫は「条件つきの1文」で減らし、残高は「企業ごとの鍵」を取ってから動かす。**

受け入れ基準 K1 の負荷テスト（`scripts/check-load.mjs`、60社・375件を同時に投げる）で
**3件の不具合が出た。いずれも1人ずつ操作している限り絶対に出ない。**

| 出たこと                              | 数字               | 原因                                                             |
| ------------------------------------- | ------------------ | ---------------------------------------------------------------- |
| 在庫より多く売れた（受け入れ基準 D4） | 在庫5個が **33個** | 在庫を「読む→足りるか見る→書く」の3手で減らしていた              |
| 残高が負になった（同 D3・K1）         | **−57,000 MP**     | 残高も同じ3手。同時に来た購入が両方とも「足りる」と判断した      |
| 市場全体の MP の勘定が合わない        | **73,500 MP**      | `calculateSupply` が広告費（市場の外へ出る額）を数えていなかった |

**直し方を2つに分けた理由**:

- **在庫**は「数を1つ減らすだけ」なので、`update ... where available_quantity >= ?` の
  1文にできる。データベースが行に鍵をかけるので、読みと書きの間が無くなる
- **残高**は「取引履歴を全部数えてから決める」ので1文にできない。
  払う企業ごとに `pg_advisory_xact_lock` を取り、**鍵の中で数え直してから**
  同じ取引の中で書く。鍵は**払う側だけ**（受け取る側は増えるだけなので負にならず、
  デッドロックも起きない）。別々の企業の購入は今までどおり同時に進む

**3件目は保存則そのものの誤り**だった。`granted − expired = circulating` を前提に
していたが、広告費は市場の外へ出るので合わない。広告を1つでも売ると
`pnpm run check:mp` が落ちる状態だった。CI では `check:mp` が広告の検査より先に
走るため、今まで露見していなかった。`circulating = granted − expired − spentOutside`
に直した。

**負荷テストは CI に入れた**（60社・375件で 16〜25 秒）。同時実行の不具合は
「動かしてみたら動いた」では絶対に見つからない。毎回走らせる価値がある。
